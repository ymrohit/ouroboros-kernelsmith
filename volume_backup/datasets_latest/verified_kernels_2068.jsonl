{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h)\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=1024)\n    return y\n", "khash": "70ed08bdcc774dd0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h)\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=256)\n    return y\n", "khash": "f4c34311448efddc", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h)\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=512)\n    return y\n", "khash": "6ea422d38bd245d5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h)\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=2048)\n    return y\n", "khash": "2c96afcb4646d5ed", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h)\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=4096)\n    return y\n", "khash": "f966ee7a9f9074b9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h)\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "f91ee6443d753772", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h)\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "81c6738a80c64a89", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h)\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "6ca44586f428c4a2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h)\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=256, num_warps=4)\n    return y\n", "khash": "25ca82e132b80fe1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h)\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=256, num_warps=8)\n    return y\n", "khash": "424cb56172bbac67", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h)\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=256, num_stages=2)\n    return y\n", "khash": "2d17ebffda4b9639", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h)\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=512, num_warps=4)\n    return y\n", "khash": "39b8cc632e9d8d70", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h)\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=512, num_warps=8)\n    return y\n", "khash": "7e60e50de4816c3c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h)\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=512, num_stages=2)\n    return y\n", "khash": "51213ac479a4df97", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h)\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "48c825dd7e93bbb7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h)\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "515c82d8b84eacf6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h)\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "020eacb644a19ae7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h)\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "96b4b805b8dc2252", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h)\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "daf4ab549289c132", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h)\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "2b860b62ad2c950b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); m = cols < N\n    h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n        tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(m, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, hc * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "6ae377616ba62049", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); m = cols < N\n    h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n        tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(m, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, hc * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "0f4b11847bf9b619", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); m = cols < N\n    h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n        tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(m, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, hc * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "6fd004c085643d1e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); m = cols < N\n    h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n        tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(m, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, hc * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "7dcc18cb742cc020", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += tl.where(m, h, 0.0)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); vacc += d * d\n    rr = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=1024)\n    return y\n", "khash": "55c1acecfbe129e3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += tl.where(m, h, 0.0)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); vacc += d * d\n    rr = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=256)\n    return y\n", "khash": "3240de13eb4c95d4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += tl.where(m, h, 0.0)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); vacc += d * d\n    rr = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=512)\n    return y\n", "khash": "4e74b6d6fcf339c8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += tl.where(m, h, 0.0)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); vacc += d * d\n    rr = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=2048)\n    return y\n", "khash": "6a96ba3c5cf50b60", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += tl.where(m, h, 0.0)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); vacc += d * d\n    rr = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=4096)\n    return y\n", "khash": "6ac1accabf6b8d4d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += tl.where(m, h, 0.0)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); vacc += d * d\n    rr = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "03ffdade6e755275", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += tl.where(m, h, 0.0)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); vacc += d * d\n    rr = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "e5313dffbf7ef278", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += tl.where(m, h, 0.0)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); vacc += d * d\n    rr = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "bd7906d6fbd715fc", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += tl.where(m, h, 0.0)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); vacc += d * d\n    rr = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=256, num_warps=4)\n    return y\n", "khash": "601f64fef92c812c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += tl.where(m, h, 0.0)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); vacc += d * d\n    rr = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=256, num_warps=8)\n    return y\n", "khash": "1bbb243f20084a4f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += tl.where(m, h, 0.0)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); vacc += d * d\n    rr = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=256, num_stages=2)\n    return y\n", "khash": "b55dcffd4d701e3b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += tl.where(m, h, 0.0)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); vacc += d * d\n    rr = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=512, num_warps=4)\n    return y\n", "khash": "f3e04fb4cefbf945", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += tl.where(m, h, 0.0)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); vacc += d * d\n    rr = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=512, num_warps=8)\n    return y\n", "khash": "8c6528248d54c277", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += tl.where(m, h, 0.0)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); vacc += d * d\n    rr = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=512, num_stages=2)\n    return y\n", "khash": "b828e397a8a8f088", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += tl.where(m, h, 0.0)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); vacc += d * d\n    rr = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "7c473be7b733be4a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += tl.where(m, h, 0.0)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); vacc += d * d\n    rr = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "fa995463cc2c16f5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += tl.where(m, h, 0.0)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); vacc += d * d\n    rr = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "b317eb2f4e08e00c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += tl.where(m, h, 0.0)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); vacc += d * d\n    rr = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "5c6bc2965279d871", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += tl.where(m, h, 0.0)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); vacc += d * d\n    rr = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "8573611ba8f4cb40", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += tl.where(m, h, 0.0)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, h - mu, 0.0); vacc += d * d\n    rr = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (h - mu) * rr * w + b, mask=m)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-5, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "14bbd86fa822df0a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024)\n    return y\n", "khash": "015bb37f97bfbe9f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256)\n    return y\n", "khash": "e7e60c48d6436566", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512)\n    return y\n", "khash": "f65eb6a44ab2cfbb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048)\n    return y\n", "khash": "283e4a31ffb4b715", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096)\n    return y\n", "khash": "75621a63d3228f46", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "fdd46906a51b696b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "864e0c6ea6c30654", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "b8ab47eb4e5f3fa7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=4)\n    return y\n", "khash": "417fe109547c72c7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=8)\n    return y\n", "khash": "2e3ee8dd9b0d16a4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_stages=2)\n    return y\n", "khash": "f6ca3556c06262fb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=4)\n    return y\n", "khash": "1ce7f62634982351", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=8)\n    return y\n", "khash": "4a490a002a127c3a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_stages=2)\n    return y\n", "khash": "b0c95310f3b939a2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "485e0ec0271de77b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "2a4ee23c3aed2f11", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "8ac3e8d94a161a97", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "0bbfb2ec60951e7d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "6e2a14d8a850443b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "cff86292040160f7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "895538435287ad11", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "4cd718bad288408d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "ccfee8e724f8856c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "426337d2f0ff2fec", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024)\n    return y\n", "khash": "f75b05edf3392be8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256)\n    return y\n", "khash": "cffbd0d6e66e5d1c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512)\n    return y\n", "khash": "f867c23e2d1ae6b7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048)\n    return y\n", "khash": "a7c1d9260d5557e5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096)\n    return y\n", "khash": "e2f9223414fa17d9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "ba3f155fd9a066b7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "ba7a8e837a3384af", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "0c1e0f8019e345db", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=4)\n    return y\n", "khash": "3b32b3136a331da9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=8)\n    return y\n", "khash": "d80e5421016a5a70", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_stages=2)\n    return y\n", "khash": "c37132efe44da432", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=4)\n    return y\n", "khash": "870d9c71f73004ec", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=8)\n    return y\n", "khash": "d9f3df6aab65b5fb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_stages=2)\n    return y\n", "khash": "07ed2f00f662dfe8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "ff3d66858c7f2c9a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "20affa708ae35258", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "7047718cbc83ecff", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "0baf14fee4573ada", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "ae7dc28e75c51600", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "095600ae2bc03811", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "1726c38e24e39370", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "3d883ba6186fcdce", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "9caff4474093c15b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "10f7cbac11e67c90", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024)\n    return y\n", "khash": "ae2ee5453d483281", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256)\n    return y\n", "khash": "9e670d97c0a95869", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512)\n    return y\n", "khash": "6e8e521d3cd18c9d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048)\n    return y\n", "khash": "d38cf4c6e76c7440", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096)\n    return y\n", "khash": "648cca88d7d3a126", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "0f845e689f1454ac", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "64f93d6dbf55fdcf", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "b38f9a23ba304b97", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=4)\n    return y\n", "khash": "782589fa4071fb0b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=8)\n    return y\n", "khash": "44204e85b817f478", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_stages=2)\n    return y\n", "khash": "528af4af7d80d3df", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=4)\n    return y\n", "khash": "26058e772ced24e0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=8)\n    return y\n", "khash": "cf4d1ce223dc02fc", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_stages=2)\n    return y\n", "khash": "91afa6f19c2ad536", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "b099f54c04d79f3d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "6868663542d943eb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "52893b0a7a9a0eb8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "2f864c6adb853af1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "2244e6a2d08a437d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "8b5e21e864a7e94b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "db8c25836f4b6056", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "6926012cabdb88e5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "232e37c578685c1a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "6f5f4b1b4bd5f193", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += h * h\n    rr = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=1024)\n    return y\n", "khash": "44bdb97236b02488", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += h * h\n    rr = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=256)\n    return y\n", "khash": "21b05172b682611f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += h * h\n    rr = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=512)\n    return y\n", "khash": "87ea17b68bda14e8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += h * h\n    rr = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=2048)\n    return y\n", "khash": "10138db22b159a3b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += h * h\n    rr = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=4096)\n    return y\n", "khash": "687dc8835c2269bf", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += h * h\n    rr = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "707c1bf89f126eca", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += h * h\n    rr = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "954f8a152bca5a45", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += h * h\n    rr = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "13398fdd2d53ebc1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += h * h\n    rr = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=256, num_warps=4)\n    return y\n", "khash": "77bc858d66c1f790", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += h * h\n    rr = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=256, num_warps=8)\n    return y\n", "khash": "a2586e14c9b5ee66", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += h * h\n    rr = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=256, num_stages=2)\n    return y\n", "khash": "c60c5de052b75833", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += h * h\n    rr = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=512, num_warps=4)\n    return y\n", "khash": "360fd08177aee86e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += h * h\n    rr = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=512, num_warps=8)\n    return y\n", "khash": "a6a94dd95a32d462", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += h * h\n    rr = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=512, num_stages=2)\n    return y\n", "khash": "078dc49a669d20d6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += h * h\n    rr = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "0c0858e9b91678f4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += h * h\n    rr = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "9b75cc6b369c6aad", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += h * h\n    rr = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "436edbdf5f7ddbc5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += h * h\n    rr = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "fdd86ad7e032150a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += h * h\n    rr = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "3c9d885185f9e307", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        acc += h * h\n    rr = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "cfdcddb2929db4e5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); cols = tl.arange(0, BLOCK); m = cols < N\n    h = tl.load(x_ptr + row * stride + cols, mask=m, other=0.0).to(tl.float32) + \\\n        tl.load(r_ptr + row * stride + cols, mask=m, other=0.0).to(tl.float32)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    tl.store(y_ptr + row * stride + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "7e8aa59cdb5656d5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); cols = tl.arange(0, BLOCK); m = cols < N\n    h = tl.load(x_ptr + row * stride + cols, mask=m, other=0.0).to(tl.float32) + \\\n        tl.load(r_ptr + row * stride + cols, mask=m, other=0.0).to(tl.float32)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    tl.store(y_ptr + row * stride + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "2e6db1d33d77b2b8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); cols = tl.arange(0, BLOCK); m = cols < N\n    h = tl.load(x_ptr + row * stride + cols, mask=m, other=0.0).to(tl.float32) + \\\n        tl.load(r_ptr + row * stride + cols, mask=m, other=0.0).to(tl.float32)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    tl.store(y_ptr + row * stride + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "1cdf21f6f7a720c8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); cols = tl.arange(0, BLOCK); m = cols < N\n    h = tl.load(x_ptr + row * stride + cols, mask=m, other=0.0).to(tl.float32) + \\\n        tl.load(r_ptr + row * stride + cols, mask=m, other=0.0).to(tl.float32)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    tl.store(y_ptr + row * stride + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "6167cf1636ea2ae7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=1024)\n    return y\n", "khash": "ee8d38fd9bb16cdd", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=256)\n    return y\n", "khash": "7bc4fa4344316d5b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=512)\n    return y\n", "khash": "d2e933187c70b1a4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=2048)\n    return y\n", "khash": "9aa7ff8146c9173f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=4096)\n    return y\n", "khash": "d18a2bf77ec21fe1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "80388e532bf55346", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "e0b2de991f270dcf", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "44c75e1985dd96df", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=256, num_warps=4)\n    return y\n", "khash": "9ad0a82fe8f700f6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=256, num_warps=8)\n    return y\n", "khash": "93d17b8892df4b8c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=256, num_stages=2)\n    return y\n", "khash": "d29f71174de0c031", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=512, num_warps=4)\n    return y\n", "khash": "a4e978e143b1e42b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=512, num_warps=8)\n    return y\n", "khash": "b1d931406d82438d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=512, num_stages=2)\n    return y\n", "khash": "aff69b73ac4ffe5e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "70d13e5de89c9b56", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "bd79f21d916b8c7b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "05dddc8abbafcf36", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "5724794fe252b0b5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "e3feab44316ea3f9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        h = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, h * rr * w, mask=m)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-6, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "d5aeac5d64fd7441", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024)\n    return y\n", "khash": "ad0e653828e99c68", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256)\n    return y\n", "khash": "9cdd7e837b1c6ea0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512)\n    return y\n", "khash": "258d610612394450", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048)\n    return y\n", "khash": "dfcedb876b7b0fd8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096)\n    return y\n", "khash": "e8bc7618dd2f70b6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "3f6f7190e7f63097", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "caedc34aa807a7f0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "dc9f4a9eb1b65fbd", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=4)\n    return y\n", "khash": "e646ed32e6f5f632", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=8)\n    return y\n", "khash": "3cc6c4e151b559d8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_stages=2)\n    return y\n", "khash": "8205c8674cb882b2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=4)\n    return y\n", "khash": "c3ecba13c97a92c3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=8)\n    return y\n", "khash": "e6d9a88ee1d07daa", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_stages=2)\n    return y\n", "khash": "79638aeb9f432b1e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "fe441ec1efd34f9f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "be38ebf169804615", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "64b6213258eaddef", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "902a5e8bec9698d1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "8c61215d4b21b1d3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "d2a959657a40c465", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "371ccb54963647e1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "6dac07cd97b04499", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "4c43d13d9a54b699", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "a98095d7fe3221c2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024)\n    return y\n", "khash": "bdc060dcbba6e5d3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256)\n    return y\n", "khash": "6f4cd6afc307ef7b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512)\n    return y\n", "khash": "c325ddd4bc66fdb9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048)\n    return y\n", "khash": "1a0e1604f23edbbe", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096)\n    return y\n", "khash": "8b6087188dc48931", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "f01421e7b4167e89", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "5fa293331914522b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "ab9bb8e4597c462e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=4)\n    return y\n", "khash": "7ea1d1c31cab1712", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=8)\n    return y\n", "khash": "aed8d2a85e7b3ae2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_stages=2)\n    return y\n", "khash": "d2200fa66552cf7b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=4)\n    return y\n", "khash": "5a1c6831f7acb305", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=8)\n    return y\n", "khash": "bf6f1a6480e399f4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_stages=2)\n    return y\n", "khash": "2add796d564a01cf", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "5468207948a86fe1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "9b46bb1d48b37a11", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "b3e0407a8323fe64", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "11d0dcabc0c98fca", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "0daf71a5173d39c6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "d1eba9fc09d1a1aa", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "8f05279e4fe4b4e7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "91ab1cacc152ebb4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "3a504de77a384f2f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "acff1b83a3ec8940", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_rope", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; r_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2; cols = tl.arange(0, BLOCK); m = cols < D\n    hh = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    rms = tl.rsqrt(tl.sum(hh * hh) / D + eps)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    n = hh * rms * w\n    sh = tl.where(cols < h, cols + h, cols - h)\n    hs = tl.load(x_ptr + sh, mask=m, other=0.0).to(tl.float32) + tl.load(r_ptr + sh, mask=m, other=0.0).to(tl.float32)\n    ws = tl.load(w_ptr + sh, mask=m, other=0.0).to(tl.float32)\n    rot = tl.where(cols < h, -1.0, 1.0) * (hs * rms * ws)\n    cs = tl.load(cos_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    sn = tl.load(sin_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, n * cs + rot * sn, mask=m)\ndef run(x, residual, w, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, cos, sin, y, x.stride(0), D, 1e-6, BLOCK=triton.next_power_of_2(D))\n    return y\n", "khash": "80a6bd23a5f0bf8d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8, v9: tl.constexpr):\n    v1"}
{"op": "add_rmsnorm_rope", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; r_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2; cols = tl.arange(0, BLOCK); m = cols < D\n    hh = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    rms = tl.rsqrt(tl.sum(hh * hh) / D + eps)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    n = hh * rms * w\n    sh = tl.where(cols < h, cols + h, cols - h)\n    hs = tl.load(x_ptr + sh, mask=m, other=0.0).to(tl.float32) + tl.load(r_ptr + sh, mask=m, other=0.0).to(tl.float32)\n    ws = tl.load(w_ptr + sh, mask=m, other=0.0).to(tl.float32)\n    rot = tl.where(cols < h, -1.0, 1.0) * (hs * rms * ws)\n    cs = tl.load(cos_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    sn = tl.load(sin_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, n * cs + rot * sn, mask=m)\ndef run(x, residual, w, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, cos, sin, y, x.stride(0), D, 1e-6, BLOCK=triton.next_power_of_2(D), num_warps=4)\n    return y\n", "khash": "63f4857425465aaf", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8, v9: tl.constexpr):\n    v1"}
{"op": "add_rmsnorm_rope", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; r_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2; cols = tl.arange(0, BLOCK); m = cols < D\n    hh = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    rms = tl.rsqrt(tl.sum(hh * hh) / D + eps)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    n = hh * rms * w\n    sh = tl.where(cols < h, cols + h, cols - h)\n    hs = tl.load(x_ptr + sh, mask=m, other=0.0).to(tl.float32) + tl.load(r_ptr + sh, mask=m, other=0.0).to(tl.float32)\n    ws = tl.load(w_ptr + sh, mask=m, other=0.0).to(tl.float32)\n    rot = tl.where(cols < h, -1.0, 1.0) * (hs * rms * ws)\n    cs = tl.load(cos_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    sn = tl.load(sin_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, n * cs + rot * sn, mask=m)\ndef run(x, residual, w, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, cos, sin, y, x.stride(0), D, 1e-6, BLOCK=triton.next_power_of_2(D), num_warps=8)\n    return y\n", "khash": "99d686aded23e283", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8, v9: tl.constexpr):\n    v1"}
{"op": "add_rmsnorm_rope", "src": "\n@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; r_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2; cols = tl.arange(0, BLOCK); m = cols < D\n    hh = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    rms = tl.rsqrt(tl.sum(hh * hh) / D + eps)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    n = hh * rms * w\n    sh = tl.where(cols < h, cols + h, cols - h)\n    hs = tl.load(x_ptr + sh, mask=m, other=0.0).to(tl.float32) + tl.load(r_ptr + sh, mask=m, other=0.0).to(tl.float32)\n    ws = tl.load(w_ptr + sh, mask=m, other=0.0).to(tl.float32)\n    rot = tl.where(cols < h, -1.0, 1.0) * (hs * rms * ws)\n    cs = tl.load(cos_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    sn = tl.load(sin_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, n * cs + rot * sn, mask=m)\ndef run(x, residual, w, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, cos, sin, y, x.stride(0), D, 1e-6, BLOCK=triton.next_power_of_2(D), num_stages=2)\n    return y\n", "khash": "b30882a8316c0873", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8, v9: tl.constexpr):\n    v1"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024)\n    return y\n", "khash": "0a0ca53d5893b9a8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256)\n    return y\n", "khash": "734a23c2a01577c9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512)\n    return y\n", "khash": "d2a1dae9d6901a7e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048)\n    return y\n", "khash": "d78b5f64fb19099d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096)\n    return y\n", "khash": "3795c6b981a02dc6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "e86c0a9858f40444", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "8565a5dc2b160cf5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "0582d2a4bdab48e8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=4)\n    return y\n", "khash": "dff9ef69ed3fb386", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=8)\n    return y\n", "khash": "96f6053b18ea9a1d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_stages=2)\n    return y\n", "khash": "43e842bdaf80b3e4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=4)\n    return y\n", "khash": "2debd10b7fa1c7ac", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=8)\n    return y\n", "khash": "864dec45753d6ecd", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_stages=2)\n    return y\n", "khash": "cdba374eae530129", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "5d300fcc815b610c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "f7b47e150c35e2b1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "62c448c0620c5f12", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "b225b278e2f6e081", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "4a678b89822dca28", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "ecc9726e9dc7a53a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "bd49e4908de48fa8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "8012b02eec549f1e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "de6f682b0001dae3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "fa284f8abe18ecef", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "bias_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, b_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (x + 0.044715 * x * x * x)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, bias):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, bias, y, x.stride(0), N, BLOCK=1024)\n    return y\n", "khash": "de47ffeb7ce6b457", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "bias_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, b_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (x + 0.044715 * x * x * x)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, bias):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, bias, y, x.stride(0), N, BLOCK=256)\n    return y\n", "khash": "dde41cc7ea835a0c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "bias_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, b_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (x + 0.044715 * x * x * x)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, bias):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, bias, y, x.stride(0), N, BLOCK=512)\n    return y\n", "khash": "71e5d0e70aed2df4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "bias_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, b_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (x + 0.044715 * x * x * x)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, bias):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, bias, y, x.stride(0), N, BLOCK=2048)\n    return y\n", "khash": "3eaaecf2b7bde812", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "bias_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, b_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (x + 0.044715 * x * x * x)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, bias):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, bias, y, x.stride(0), N, BLOCK=4096)\n    return y\n", "khash": "2d321ce2df72014b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "bias_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, b_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (x + 0.044715 * x * x * x)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, bias):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, bias, y, x.stride(0), N, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "225926d7f125c694", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "bias_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, b_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (x + 0.044715 * x * x * x)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, bias):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, bias, y, x.stride(0), N, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "8c1eecfa45451f53", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "bias_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, b_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (x + 0.044715 * x * x * x)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, bias):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, bias, y, x.stride(0), N, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "989db3edb0ca117d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "bias_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, b_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (x + 0.044715 * x * x * x)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, bias):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, bias, y, x.stride(0), N, BLOCK=256, num_warps=4)\n    return y\n", "khash": "8a810b114baa0fe7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "bias_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, b_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (x + 0.044715 * x * x * x)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, bias):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, bias, y, x.stride(0), N, BLOCK=256, num_warps=8)\n    return y\n", "khash": "42df281d36ca52d5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "bias_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, b_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (x + 0.044715 * x * x * x)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, bias):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, bias, y, x.stride(0), N, BLOCK=256, num_stages=2)\n    return y\n", "khash": "1560c9855454f8d7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "bias_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, b_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (x + 0.044715 * x * x * x)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, bias):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, bias, y, x.stride(0), N, BLOCK=512, num_warps=4)\n    return y\n", "khash": "e293469d6bd2ff40", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "bias_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, b_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (x + 0.044715 * x * x * x)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, bias):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, bias, y, x.stride(0), N, BLOCK=512, num_warps=8)\n    return y\n", "khash": "9761e3280670f517", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "bias_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, b_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (x + 0.044715 * x * x * x)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, bias):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, bias, y, x.stride(0), N, BLOCK=512, num_stages=2)\n    return y\n", "khash": "7916fcc9902619f7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "bias_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, b_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (x + 0.044715 * x * x * x)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, bias):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, bias, y, x.stride(0), N, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "4a3f26ea7e4f57c6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "bias_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, b_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (x + 0.044715 * x * x * x)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, bias):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, bias, y, x.stride(0), N, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "2d611f8486390864", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "bias_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, b_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (x + 0.044715 * x * x * x)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, bias):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, bias, y, x.stride(0), N, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "37b94beaa57abc90", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "bias_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, b_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (x + 0.044715 * x * x * x)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, bias):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, bias, y, x.stride(0), N, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "67496553631d25cb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "bias_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, b_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (x + 0.044715 * x * x * x)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, bias):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, bias, y, x.stride(0), N, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "9615495688cf006d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "bias_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, b_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) + \\\n            tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (x + 0.044715 * x * x * x)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, bias):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, bias, y, x.stride(0), N, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "78adc5efd0551f52", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); q_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        q = tl.load(q_ptr + cols, mask=m, other=0).to(tl.float32)\n        tl.store(y_ptr + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=1024)\n    return y\n", "khash": "0ff99b14db36710a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); q_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        q = tl.load(q_ptr + cols, mask=m, other=0).to(tl.float32)\n        tl.store(y_ptr + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=256)\n    return y\n", "khash": "96eec26754ef803a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); q_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        q = tl.load(q_ptr + cols, mask=m, other=0).to(tl.float32)\n        tl.store(y_ptr + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=512)\n    return y\n", "khash": "67108e337e4fdb77", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); q_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        q = tl.load(q_ptr + cols, mask=m, other=0).to(tl.float32)\n        tl.store(y_ptr + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=2048)\n    return y\n", "khash": "e5134a2a640cc0b1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); q_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        q = tl.load(q_ptr + cols, mask=m, other=0).to(tl.float32)\n        tl.store(y_ptr + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=4096)\n    return y\n", "khash": "b5e63e9e2744748b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); q_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        q = tl.load(q_ptr + cols, mask=m, other=0).to(tl.float32)\n        tl.store(y_ptr + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "7e320d32807d7c46", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); q_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        q = tl.load(q_ptr + cols, mask=m, other=0).to(tl.float32)\n        tl.store(y_ptr + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "75c9bfe39a5962a1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); q_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        q = tl.load(q_ptr + cols, mask=m, other=0).to(tl.float32)\n        tl.store(y_ptr + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "fd8bc215970da05a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); q_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        q = tl.load(q_ptr + cols, mask=m, other=0).to(tl.float32)\n        tl.store(y_ptr + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=256, num_warps=4)\n    return y\n", "khash": "3efde2940c8ec708", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); q_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        q = tl.load(q_ptr + cols, mask=m, other=0).to(tl.float32)\n        tl.store(y_ptr + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=256, num_warps=8)\n    return y\n", "khash": "72834972134ef055", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); q_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        q = tl.load(q_ptr + cols, mask=m, other=0).to(tl.float32)\n        tl.store(y_ptr + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=256, num_stages=2)\n    return y\n", "khash": "faa197ae11214899", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); q_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        q = tl.load(q_ptr + cols, mask=m, other=0).to(tl.float32)\n        tl.store(y_ptr + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=512, num_warps=4)\n    return y\n", "khash": "9beaa28638632e11", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); q_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        q = tl.load(q_ptr + cols, mask=m, other=0).to(tl.float32)\n        tl.store(y_ptr + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=512, num_warps=8)\n    return y\n", "khash": "d5128f36c9f1399e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); q_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        q = tl.load(q_ptr + cols, mask=m, other=0).to(tl.float32)\n        tl.store(y_ptr + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=512, num_stages=2)\n    return y\n", "khash": "ff3569c978598c3d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); q_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        q = tl.load(q_ptr + cols, mask=m, other=0).to(tl.float32)\n        tl.store(y_ptr + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "50c2fc999c32accf", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); q_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        q = tl.load(q_ptr + cols, mask=m, other=0).to(tl.float32)\n        tl.store(y_ptr + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "fc6f2da81788bac0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); q_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        q = tl.load(q_ptr + cols, mask=m, other=0).to(tl.float32)\n        tl.store(y_ptr + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "9b29d6c7f81ab520", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); q_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        q = tl.load(q_ptr + cols, mask=m, other=0).to(tl.float32)\n        tl.store(y_ptr + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "7d53acb043d0a1a8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); q_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        q = tl.load(q_ptr + cols, mask=m, other=0).to(tl.float32)\n        tl.store(y_ptr + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "d142e402cbe7be71", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); q_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        q = tl.load(q_ptr + cols, mask=m, other=0).to(tl.float32)\n        tl.store(y_ptr + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "cbb0c3343aa18cbd", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    cols = tl.arange(0, BLOCK); m = cols < N\n    q = tl.load(q_ptr + row * stride + cols, mask=m, other=0).to(tl.float32)\n    tl.store(y_ptr + row * stride + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "779701f5cffd7901", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    cols = tl.arange(0, BLOCK); m = cols < N\n    q = tl.load(q_ptr + row * stride + cols, mask=m, other=0).to(tl.float32)\n    tl.store(y_ptr + row * stride + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "0914262a80fe7318", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    cols = tl.arange(0, BLOCK); m = cols < N\n    q = tl.load(q_ptr + row * stride + cols, mask=m, other=0).to(tl.float32)\n    tl.store(y_ptr + row * stride + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "dcc672c690ed4746", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "dequant_int8", "src": "\n@triton.jit\ndef _k(q_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    sc = tl.load(s_ptr + row).to(tl.float32)\n    cols = tl.arange(0, BLOCK); m = cols < N\n    q = tl.load(q_ptr + row * stride + cols, mask=m, other=0).to(tl.float32)\n    tl.store(y_ptr + row * stride + cols, q * sc, mask=m)\ndef run(q, scale):\n    M, N = q.shape\n    y = torch.empty((M, N), device=q.device, dtype=torch.float16)\n    _k[(M,)](q, scale, y, q.stride(0), N, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "816bd9c7fe23b51b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + 2.0 * tl.sigmoid(2.0 * a) - 1.0)\n    tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=1024)\n    return y\n", "khash": "8c5d7bc8e1b7f0a7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + 2.0 * tl.sigmoid(2.0 * a) - 1.0)\n    tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=2048)\n    return y\n", "khash": "ad6f6d8cb6375e24", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + 2.0 * tl.sigmoid(2.0 * a) - 1.0)\n    tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=4096)\n    return y\n", "khash": "263363aeeb363bca", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + 2.0 * tl.sigmoid(2.0 * a) - 1.0)\n    tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "e86a1b8e4a8f648d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + 2.0 * tl.sigmoid(2.0 * a) - 1.0)\n    tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "5ecc18a70f39a6aa", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + 2.0 * tl.sigmoid(2.0 * a) - 1.0)\n    tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "a6a383fbe2e41fbe", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + 2.0 * tl.sigmoid(2.0 * a) - 1.0)\n    tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "b05e8958da23fa65", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + 2.0 * tl.sigmoid(2.0 * a) - 1.0)\n    tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "b0db053d75565044", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + 2.0 * tl.sigmoid(2.0 * a) - 1.0)\n    tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "8b843f650f0c3394", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + 2.0 * tl.sigmoid(2.0 * a) - 1.0)\n    tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "77991ed0a22afc6e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + 2.0 * tl.sigmoid(2.0 * a) - 1.0)\n    tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "f11735a68bc69467", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + 2.0 * tl.sigmoid(2.0 * a) - 1.0)\n    tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "d04aa8e8c0772eca", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n        gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n        tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=1024)\n    return y\n", "khash": "a850be37178dfef1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n        gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n        tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=256)\n    return y\n", "khash": "0276123f7f441bc9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n        gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n        tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=512)\n    return y\n", "khash": "a9de900f666924f3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n        gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n        tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=2048)\n    return y\n", "khash": "bd390eed775ed023", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n        gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n        tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=4096)\n    return y\n", "khash": "59f0bd712919ed51", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n        gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n        tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "e18271285438917d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n        gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n        tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "94feb3957d4a8dcd", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n        gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n        tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "442dc7a48f3ccdf7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n        gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n        tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=256, num_warps=4)\n    return y\n", "khash": "4462af4fbfa56530", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n        gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n        tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=256, num_warps=8)\n    return y\n", "khash": "e5ea4198def23651", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n        gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n        tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=256, num_stages=2)\n    return y\n", "khash": "afb962235e6fa70d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n        gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n        tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=512, num_warps=4)\n    return y\n", "khash": "f7b6235528c34681", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n        gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n        tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=512, num_warps=8)\n    return y\n", "khash": "7a42ad21481eb2f4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n        gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n        tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=512, num_stages=2)\n    return y\n", "khash": "64e3b921877f114b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n        gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n        tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "92fed2be6b5d6408", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n        gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n        tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "1183ec64732248c2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n        gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n        tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "2f75afe62ae868ac", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n        gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n        tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "f768841386c2f439", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n        gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n        tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "603c802cfd24872c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n        gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n        tl.store(y_ptr + cols, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "ca434dc3afd8fdf2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); col = tl.program_id(1) * BLOCK + tl.arange(0, BLOCK); m = col < N\n    base = row * stride\n    g = tl.load(g_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n    tl.store(y_ptr + base + col, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M, triton.cdiv(N, 1024))](gate, up, y, gate.stride(0), N, BLOCK=1024)\n    return y\n", "khash": "06cdf398093a8a2a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); col = tl.program_id(1) * BLOCK + tl.arange(0, BLOCK); m = col < N\n    base = row * stride\n    g = tl.load(g_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n    tl.store(y_ptr + base + col, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M, triton.cdiv(N, 1024))](gate, up, y, gate.stride(0), N, BLOCK=2048)\n    return y\n", "khash": "ca1825fd212da579", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); col = tl.program_id(1) * BLOCK + tl.arange(0, BLOCK); m = col < N\n    base = row * stride\n    g = tl.load(g_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n    tl.store(y_ptr + base + col, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M, triton.cdiv(N, 1024))](gate, up, y, gate.stride(0), N, BLOCK=4096)\n    return y\n", "khash": "cc5a7624858a19d3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); col = tl.program_id(1) * BLOCK + tl.arange(0, BLOCK); m = col < N\n    base = row * stride\n    g = tl.load(g_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n    tl.store(y_ptr + base + col, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M, triton.cdiv(N, 1024))](gate, up, y, gate.stride(0), N, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "7b1eacb1125fdeff", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); col = tl.program_id(1) * BLOCK + tl.arange(0, BLOCK); m = col < N\n    base = row * stride\n    g = tl.load(g_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n    tl.store(y_ptr + base + col, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M, triton.cdiv(N, 1024))](gate, up, y, gate.stride(0), N, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "f5ea033abcbe252e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); col = tl.program_id(1) * BLOCK + tl.arange(0, BLOCK); m = col < N\n    base = row * stride\n    g = tl.load(g_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n    tl.store(y_ptr + base + col, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M, triton.cdiv(N, 1024))](gate, up, y, gate.stride(0), N, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "1ff08cea19eb74bb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); col = tl.program_id(1) * BLOCK + tl.arange(0, BLOCK); m = col < N\n    base = row * stride\n    g = tl.load(g_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n    tl.store(y_ptr + base + col, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M, triton.cdiv(N, 1024))](gate, up, y, gate.stride(0), N, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "cbc86013a13a8491", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); col = tl.program_id(1) * BLOCK + tl.arange(0, BLOCK); m = col < N\n    base = row * stride\n    g = tl.load(g_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n    tl.store(y_ptr + base + col, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M, triton.cdiv(N, 1024))](gate, up, y, gate.stride(0), N, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "e37d2451c6ea560d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); col = tl.program_id(1) * BLOCK + tl.arange(0, BLOCK); m = col < N\n    base = row * stride\n    g = tl.load(g_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n    tl.store(y_ptr + base + col, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M, triton.cdiv(N, 1024))](gate, up, y, gate.stride(0), N, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "e8319b21b78a9ab4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); col = tl.program_id(1) * BLOCK + tl.arange(0, BLOCK); m = col < N\n    base = row * stride\n    g = tl.load(g_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n    tl.store(y_ptr + base + col, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M, triton.cdiv(N, 1024))](gate, up, y, gate.stride(0), N, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "310ab2bcb3c2f0e4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); col = tl.program_id(1) * BLOCK + tl.arange(0, BLOCK); m = col < N\n    base = row * stride\n    g = tl.load(g_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n    tl.store(y_ptr + base + col, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M, triton.cdiv(N, 1024))](gate, up, y, gate.stride(0), N, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "ff36439a87b8efb5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "geglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); col = tl.program_id(1) * BLOCK + tl.arange(0, BLOCK); m = col < N\n    base = row * stride\n    g = tl.load(g_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + base + col, mask=m, other=0.0).to(tl.float32)\n    a = 0.7978845608028654 * (g + 0.044715 * g * g * g)\n    gelu = 0.5 * g * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0))\n    tl.store(y_ptr + base + col, gelu * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M, triton.cdiv(N, 1024))](gate, up, y, gate.stride(0), N, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "87bc72d3badc4d13", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=1024)\n    return y\n", "khash": "5f9c03f9de38e48c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=2048)\n    return y\n", "khash": "ba06a4102d477b43", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=4096)\n    return y\n", "khash": "9ac41c426202a380", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "d97c45932e4bc1f9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "b7aa18bd99115c21", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "8dceaf75e21a77a8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "d95b955653ec216a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "038e5f38d54b0e70", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "d45eac7c411290c3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "88d0a9211e9ef5fc", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "3dc6b6e21510cbd1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "843d953ffc8b36ee", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024)\n    return y\n", "khash": "94e5b868f889dc47", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256)\n    return y\n", "khash": "5ba8effb8384c075", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512)\n    return y\n", "khash": "ac51c820fdb332e0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048)\n    return y\n", "khash": "7628a023981d9215", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096)\n    return y\n", "khash": "a0f6961c9517209f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "2e18ef2f07dc6152", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "365eadf928f9d7dc", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "54300386ee1860b0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256, num_warps=4)\n    return y\n", "khash": "7bc41a583dd81cd1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256, num_warps=8)\n    return y\n", "khash": "5a90ed986efff289", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256, num_stages=2)\n    return y\n", "khash": "b286e22428de2da9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512, num_warps=4)\n    return y\n", "khash": "6a025b58e818b05f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512, num_warps=8)\n    return y\n", "khash": "4b9418e9df063862", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512, num_stages=2)\n    return y\n", "khash": "e93ed3b9d2dccfde", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "f47619331f41a5b1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "4d83380724b45a43", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "389bf78e26c96ec3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "298dada6fe60cc13", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "d153584a15392eb8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "gelu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, 0.5 * x * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (x + 0.044715 * x * x * x))) - 1.0)), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "ef05b65ad2c64e79", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "l2norm", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x * tl.rsqrt(tl.sum(x * x) + eps), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, 1e-6, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "c9476d1364b46c8a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "l2norm", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x * tl.rsqrt(tl.sum(x * x) + eps), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, 1e-6, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "c3bb093d8fb6488b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "l2norm", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x * tl.rsqrt(tl.sum(x * x) + eps), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, 1e-6, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "7fb50e8b81dcc114", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "l2norm", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x * tl.rsqrt(tl.sum(x * x) + eps), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, 1e-6, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "a0843025e12f87b6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=1024)\n    return y\n", "khash": "2ff9e0b10e496966", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=256)\n    return y\n", "khash": "91afb9810a0755af", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=512)\n    return y\n", "khash": "6985ca6df6a5783f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=2048)\n    return y\n", "khash": "5ae6f564268d9dce", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=4096)\n    return y\n", "khash": "f9e69e11f03d21cf", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "739142f4a9b9e0d6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "7a5a2d24fba7d470", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "9bb1860771cebfc6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=256, num_warps=4)\n    return y\n", "khash": "bb6d5411458418f9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=256, num_warps=8)\n    return y\n", "khash": "9144a18aaf39ceae", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=256, num_stages=2)\n    return y\n", "khash": "1aab4e2beb684778", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=512, num_warps=4)\n    return y\n", "khash": "c53fd029119237a3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=512, num_warps=8)\n    return y\n", "khash": "93946d57cdeaf24d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=512, num_stages=2)\n    return y\n", "khash": "8d9cc510a211288c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "2d0e3da9c5632bc6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "5c9b2eaa57ff49a2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "30dffe3936488dd1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "85c0d2807e9fc00c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "4ba95bad8b0b4576", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "6962d79834a09557", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    mu = tl.sum(x) / N\n    xc = tl.where(m, x - mu, 0.0)\n    r = tl.rsqrt(tl.sum(xc * xc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, xc * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "20b6f8eba44e62b2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    mu = tl.sum(x) / N\n    xc = tl.where(m, x - mu, 0.0)\n    r = tl.rsqrt(tl.sum(xc * xc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, xc * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "4cc0f2b7bffebff7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    mu = tl.sum(x) / N\n    xc = tl.where(m, x - mu, 0.0)\n    r = tl.rsqrt(tl.sum(xc * xc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, xc * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "42e74077a91f8b68", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    mu = tl.sum(x) / N\n    xc = tl.where(m, x - mu, 0.0)\n    r = tl.rsqrt(tl.sum(xc * xc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, xc * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "9f64f95a5d9c7208", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        acc += tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); vacc += d * d\n    r = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=1024)\n    return y\n", "khash": "0e0f10d46d7ce2d8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        acc += tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); vacc += d * d\n    r = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=256)\n    return y\n", "khash": "e07a80391ccf79a3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        acc += tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); vacc += d * d\n    r = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=512)\n    return y\n", "khash": "0160cf7368394517", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        acc += tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); vacc += d * d\n    r = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=2048)\n    return y\n", "khash": "c21e2c0a5e7f35bb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        acc += tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); vacc += d * d\n    r = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=4096)\n    return y\n", "khash": "2f239b5191ad726d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        acc += tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); vacc += d * d\n    r = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "2ce3dc7161402db0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        acc += tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); vacc += d * d\n    r = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "7d2522d63b055a2c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        acc += tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); vacc += d * d\n    r = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "741f850d48022354", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        acc += tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); vacc += d * d\n    r = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=256, num_warps=4)\n    return y\n", "khash": "0bf0fefb65c4c5c3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        acc += tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); vacc += d * d\n    r = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=256, num_warps=8)\n    return y\n", "khash": "dbdfe9044907ade4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        acc += tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); vacc += d * d\n    r = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=256, num_stages=2)\n    return y\n", "khash": "2488ecf9b774599c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        acc += tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); vacc += d * d\n    r = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=512, num_warps=4)\n    return y\n", "khash": "dcae680db7472417", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        acc += tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); vacc += d * d\n    r = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=512, num_warps=8)\n    return y\n", "khash": "73027e20d3470b4f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        acc += tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); vacc += d * d\n    r = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=512, num_stages=2)\n    return y\n", "khash": "926516b79c94f8fb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        acc += tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); vacc += d * d\n    r = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "a13b18b8ffcf106d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        acc += tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); vacc += d * d\n    r = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "46eea7bba0196354", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        acc += tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); vacc += d * d\n    r = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "d8eb4be290e919b3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        acc += tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); vacc += d * d\n    r = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "f5b88f9ccec7d6be", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        acc += tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); vacc += d * d\n    r = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "b702f76ebf959fdd", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        acc += tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n    mu = tl.sum(acc) / N\n    vacc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        d = tl.where(m, x - mu, 0.0); vacc += d * d\n    r = tl.rsqrt(tl.sum(vacc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, (x - mu) * r * w + b, mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "928d1df49360e8ff", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        d = tl.where(m, tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) - mu, 0.0)\n        v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        ln = (x - mu) * r * w + b\n        a = 0.7978845608028654 * (ln + 0.044715 * ln * ln * ln)\n        tl.store(y_ptr + cols, 0.5 * ln * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=1024)\n    return y\n", "khash": "013b5392fe3212e9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        d = tl.where(m, tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) - mu, 0.0)\n        v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        ln = (x - mu) * r * w + b\n        a = 0.7978845608028654 * (ln + 0.044715 * ln * ln * ln)\n        tl.store(y_ptr + cols, 0.5 * ln * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=256)\n    return y\n", "khash": "02ee21adee70797b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        d = tl.where(m, tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) - mu, 0.0)\n        v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        ln = (x - mu) * r * w + b\n        a = 0.7978845608028654 * (ln + 0.044715 * ln * ln * ln)\n        tl.store(y_ptr + cols, 0.5 * ln * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=512)\n    return y\n", "khash": "7bbf501f00bd3d79", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        d = tl.where(m, tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) - mu, 0.0)\n        v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        ln = (x - mu) * r * w + b\n        a = 0.7978845608028654 * (ln + 0.044715 * ln * ln * ln)\n        tl.store(y_ptr + cols, 0.5 * ln * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=2048)\n    return y\n", "khash": "de124a4329ef1972", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        d = tl.where(m, tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) - mu, 0.0)\n        v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        ln = (x - mu) * r * w + b\n        a = 0.7978845608028654 * (ln + 0.044715 * ln * ln * ln)\n        tl.store(y_ptr + cols, 0.5 * ln * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=4096)\n    return y\n", "khash": "b840d48f21493c0e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        d = tl.where(m, tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) - mu, 0.0)\n        v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        ln = (x - mu) * r * w + b\n        a = 0.7978845608028654 * (ln + 0.044715 * ln * ln * ln)\n        tl.store(y_ptr + cols, 0.5 * ln * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "d55641c1c18dae1d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        d = tl.where(m, tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) - mu, 0.0)\n        v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        ln = (x - mu) * r * w + b\n        a = 0.7978845608028654 * (ln + 0.044715 * ln * ln * ln)\n        tl.store(y_ptr + cols, 0.5 * ln * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "c7fecc86534f33c7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        d = tl.where(m, tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) - mu, 0.0)\n        v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        ln = (x - mu) * r * w + b\n        a = 0.7978845608028654 * (ln + 0.044715 * ln * ln * ln)\n        tl.store(y_ptr + cols, 0.5 * ln * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "dac5b9afd13cb039", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        d = tl.where(m, tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) - mu, 0.0)\n        v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        ln = (x - mu) * r * w + b\n        a = 0.7978845608028654 * (ln + 0.044715 * ln * ln * ln)\n        tl.store(y_ptr + cols, 0.5 * ln * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=256, num_warps=4)\n    return y\n", "khash": "05142c9b27c9b67e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        d = tl.where(m, tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) - mu, 0.0)\n        v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        ln = (x - mu) * r * w + b\n        a = 0.7978845608028654 * (ln + 0.044715 * ln * ln * ln)\n        tl.store(y_ptr + cols, 0.5 * ln * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=256, num_warps=8)\n    return y\n", "khash": "30a0f9b9c8f8dcd7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        d = tl.where(m, tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) - mu, 0.0)\n        v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        ln = (x - mu) * r * w + b\n        a = 0.7978845608028654 * (ln + 0.044715 * ln * ln * ln)\n        tl.store(y_ptr + cols, 0.5 * ln * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=256, num_stages=2)\n    return y\n", "khash": "2b3406464a7f1b56", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        d = tl.where(m, tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) - mu, 0.0)\n        v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        ln = (x - mu) * r * w + b\n        a = 0.7978845608028654 * (ln + 0.044715 * ln * ln * ln)\n        tl.store(y_ptr + cols, 0.5 * ln * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=512, num_warps=4)\n    return y\n", "khash": "d2c03109c1bee428", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        d = tl.where(m, tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) - mu, 0.0)\n        v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        ln = (x - mu) * r * w + b\n        a = 0.7978845608028654 * (ln + 0.044715 * ln * ln * ln)\n        tl.store(y_ptr + cols, 0.5 * ln * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=512, num_warps=8)\n    return y\n", "khash": "6b48b1f5168e0c3c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        d = tl.where(m, tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) - mu, 0.0)\n        v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        ln = (x - mu) * r * w + b\n        a = 0.7978845608028654 * (ln + 0.044715 * ln * ln * ln)\n        tl.store(y_ptr + cols, 0.5 * ln * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=512, num_stages=2)\n    return y\n", "khash": "4a3630471aabe3d8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        d = tl.where(m, tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) - mu, 0.0)\n        v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        ln = (x - mu) * r * w + b\n        a = 0.7978845608028654 * (ln + 0.044715 * ln * ln * ln)\n        tl.store(y_ptr + cols, 0.5 * ln * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "b9084f4596bb65f7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        d = tl.where(m, tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) - mu, 0.0)\n        v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        ln = (x - mu) * r * w + b\n        a = 0.7978845608028654 * (ln + 0.044715 * ln * ln * ln)\n        tl.store(y_ptr + cols, 0.5 * ln * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "5d17ae94a7b2fda6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        d = tl.where(m, tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) - mu, 0.0)\n        v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        ln = (x - mu) * r * w + b\n        a = 0.7978845608028654 * (ln + 0.044715 * ln * ln * ln)\n        tl.store(y_ptr + cols, 0.5 * ln * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "750ffcd056ca7124", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        d = tl.where(m, tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) - mu, 0.0)\n        v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        ln = (x - mu) * r * w + b\n        a = 0.7978845608028654 * (ln + 0.044715 * ln * ln * ln)\n        tl.store(y_ptr + cols, 0.5 * ln * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "75b69b97714013d3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        d = tl.where(m, tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) - mu, 0.0)\n        v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        ln = (x - mu) * r * w + b\n        a = 0.7978845608028654 * (ln + 0.044715 * ln * ln * ln)\n        tl.store(y_ptr + cols, 0.5 * ln * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "c4ddb2393e64b230", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_gelu", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        s += tl.sum(tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        d = tl.where(m, tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) - mu, 0.0)\n        v += tl.sum(d * d)\n    r = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        ln = (x - mu) * r * w + b\n        a = 0.7978845608028654 * (ln + 0.044715 * ln * ln * ln)\n        tl.store(y_ptr + cols, 0.5 * ln * (1.0 + (2.0 * tl.sigmoid(2.0 * a) - 1.0)), mask=m)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-5, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "a2026a808b14f8fa", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024)\n    return y\n", "khash": "34df21a56428c076", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256)\n    return y\n", "khash": "2daacd359c05fa7e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512)\n    return y\n", "khash": "a30bf49859cf5a2a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048)\n    return y\n", "khash": "99a80652f0c50f30", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096)\n    return y\n", "khash": "45abf47f7c40efa1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "1d8594e3c35eed5a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "cdd444cbfc719343", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "f9ba3217a6a170d8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=4)\n    return y\n", "khash": "c750ad1d83879ef8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=8)\n    return y\n", "khash": "3669271ae8a1ad0a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_stages=2)\n    return y\n", "khash": "87828db8fdbd25e3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=4)\n    return y\n", "khash": "111674509b0a263c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=8)\n    return y\n", "khash": "f7cd4ef51eb93bf3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_stages=2)\n    return y\n", "khash": "a972cdcceff431a5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "165aa8bcc20a9fcf", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "6878353f5de48b3d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "fc53f7399b25cdc5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "a0d34b8a5c132b0d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "4b9f6e1fdc22aaa0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "f40316de490b0c4c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "e04cfe147c646e9f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "2e78c3ca0e44fac7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "4993f9b0b922ea06", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "f862c3f3be83bde8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024)\n    return y\n", "khash": "3dfc76f631f51254", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256)\n    return y\n", "khash": "49bd76aead250fd0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512)\n    return y\n", "khash": "5ca85fdc22485212", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048)\n    return y\n", "khash": "f0db24cf111107ad", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096)\n    return y\n", "khash": "20ac20a7f70e51dc", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "adf1565ce4f26788", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "d83b28b88ac4aca2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "21b33a8f5f94be6d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=4)\n    return y\n", "khash": "09c8d642c78b838e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=8)\n    return y\n", "khash": "7515e7dbeda2ec97", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_stages=2)\n    return y\n", "khash": "41476265afdc54da", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=4)\n    return y\n", "khash": "714b7c7ef8e8ccac", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=8)\n    return y\n", "khash": "5717bcbde576ebfc", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_stages=2)\n    return y\n", "khash": "41164ad738fd4dfe", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "924448698b952b8a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "ce31a560a31c37fb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "0be99492538d34a4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "177aac0cd7589942", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "1dd3514f1e7fdd67", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "32917e6950aed502", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "7b7e84eaacb59369", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "603c7be13985c78e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "eeacadc390cf3b61", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "d2ea88aea67db352", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + cols, mask=m, other=-float(\"inf\")).to(tl.float32)\n    x = x - tl.max(x)\n    lse = tl.log(tl.sum(tl.where(m, tl.exp(x), 0.0)))\n    tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "c5d4d57332a5fd03", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + cols, mask=m, other=-float(\"inf\")).to(tl.float32)\n    x = x - tl.max(x)\n    lse = tl.log(tl.sum(tl.where(m, tl.exp(x), 0.0)))\n    tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "3053e030f07b15b5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + cols, mask=m, other=-float(\"inf\")).to(tl.float32)\n    x = x - tl.max(x)\n    lse = tl.log(tl.sum(tl.where(m, tl.exp(x), 0.0)))\n    tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "52e004006a8f163b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + cols, mask=m, other=-float(\"inf\")).to(tl.float32)\n    x = x - tl.max(x)\n    lse = tl.log(tl.sum(tl.where(m, tl.exp(x), 0.0)))\n    tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "a458528eea6f95da", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        mx = tl.maximum(mx, tl.max(tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)))\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        s += tl.sum(tl.where(cols < N, tl.exp(x - mx), 0.0))\n    lse = mx + tl.log(s)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024)\n    return y\n", "khash": "a45ca53075010965", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        mx = tl.maximum(mx, tl.max(tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)))\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        s += tl.sum(tl.where(cols < N, tl.exp(x - mx), 0.0))\n    lse = mx + tl.log(s)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256)\n    return y\n", "khash": "88609e584db862f1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        mx = tl.maximum(mx, tl.max(tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)))\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        s += tl.sum(tl.where(cols < N, tl.exp(x - mx), 0.0))\n    lse = mx + tl.log(s)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512)\n    return y\n", "khash": "6f6b9da492852510", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        mx = tl.maximum(mx, tl.max(tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)))\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        s += tl.sum(tl.where(cols < N, tl.exp(x - mx), 0.0))\n    lse = mx + tl.log(s)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048)\n    return y\n", "khash": "9c6a2af9c0846303", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        mx = tl.maximum(mx, tl.max(tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)))\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        s += tl.sum(tl.where(cols < N, tl.exp(x - mx), 0.0))\n    lse = mx + tl.log(s)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096)\n    return y\n", "khash": "551f362d7baa7197", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        mx = tl.maximum(mx, tl.max(tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)))\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        s += tl.sum(tl.where(cols < N, tl.exp(x - mx), 0.0))\n    lse = mx + tl.log(s)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "c305e81696eddb98", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        mx = tl.maximum(mx, tl.max(tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)))\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        s += tl.sum(tl.where(cols < N, tl.exp(x - mx), 0.0))\n    lse = mx + tl.log(s)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "77d607e482dcc1cc", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        mx = tl.maximum(mx, tl.max(tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)))\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        s += tl.sum(tl.where(cols < N, tl.exp(x - mx), 0.0))\n    lse = mx + tl.log(s)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "1f18088917e03be3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        mx = tl.maximum(mx, tl.max(tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)))\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        s += tl.sum(tl.where(cols < N, tl.exp(x - mx), 0.0))\n    lse = mx + tl.log(s)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256, num_warps=4)\n    return y\n", "khash": "5560cd74db7666cd", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        mx = tl.maximum(mx, tl.max(tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)))\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        s += tl.sum(tl.where(cols < N, tl.exp(x - mx), 0.0))\n    lse = mx + tl.log(s)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256, num_warps=8)\n    return y\n", "khash": "3a3108007054bbd0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        mx = tl.maximum(mx, tl.max(tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)))\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        s += tl.sum(tl.where(cols < N, tl.exp(x - mx), 0.0))\n    lse = mx + tl.log(s)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256, num_stages=2)\n    return y\n", "khash": "71382a9f4190e05c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        mx = tl.maximum(mx, tl.max(tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)))\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        s += tl.sum(tl.where(cols < N, tl.exp(x - mx), 0.0))\n    lse = mx + tl.log(s)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512, num_warps=4)\n    return y\n", "khash": "6038da16886aecbf", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        mx = tl.maximum(mx, tl.max(tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)))\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        s += tl.sum(tl.where(cols < N, tl.exp(x - mx), 0.0))\n    lse = mx + tl.log(s)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512, num_warps=8)\n    return y\n", "khash": "1277e4a00b767153", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        mx = tl.maximum(mx, tl.max(tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)))\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        s += tl.sum(tl.where(cols < N, tl.exp(x - mx), 0.0))\n    lse = mx + tl.log(s)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512, num_stages=2)\n    return y\n", "khash": "77f16993d1ee86f7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        mx = tl.maximum(mx, tl.max(tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)))\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        s += tl.sum(tl.where(cols < N, tl.exp(x - mx), 0.0))\n    lse = mx + tl.log(s)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "81c775251dad589c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        mx = tl.maximum(mx, tl.max(tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)))\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        s += tl.sum(tl.where(cols < N, tl.exp(x - mx), 0.0))\n    lse = mx + tl.log(s)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "85af062002cb822a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        mx = tl.maximum(mx, tl.max(tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)))\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        s += tl.sum(tl.where(cols < N, tl.exp(x - mx), 0.0))\n    lse = mx + tl.log(s)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "712801638720727b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        mx = tl.maximum(mx, tl.max(tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)))\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        s += tl.sum(tl.where(cols < N, tl.exp(x - mx), 0.0))\n    lse = mx + tl.log(s)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "d2168f579a5f13b8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        mx = tl.maximum(mx, tl.max(tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)))\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        s += tl.sum(tl.where(cols < N, tl.exp(x - mx), 0.0))\n    lse = mx + tl.log(s)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "396e665e3ba82f13", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "log_softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        mx = tl.maximum(mx, tl.max(tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)))\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        s += tl.sum(tl.where(cols < N, tl.exp(x - mx), 0.0))\n    lse = mx + tl.log(s)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x - lse, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "205d983b0da15345", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "qknorm_rope", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2; cols = tl.arange(0, BLOCK); m = cols < D\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    r = tl.rsqrt(tl.sum(x * x) / D + eps)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    n = x * r * w\n    shifted = tl.where(cols < h, cols + h, cols - h)\n    xs = tl.load(x_ptr + shifted, mask=m, other=0.0).to(tl.float32)\n    ws = tl.load(w_ptr + shifted, mask=m, other=0.0).to(tl.float32)\n    rot = tl.where(cols < h, -1.0, 1.0) * (xs * r * ws)\n    cs = tl.load(cos_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    sn = tl.load(sin_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, n * cs + rot * sn, mask=m)\ndef run(x, w, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, cos, sin, y, x.stride(0), D, 1e-6, BLOCK=triton.next_power_of_2(D))\n    return y\n", "khash": "c290e622d19676aa", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "qknorm_rope", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2; cols = tl.arange(0, BLOCK); m = cols < D\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    r = tl.rsqrt(tl.sum(x * x) / D + eps)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    n = x * r * w\n    shifted = tl.where(cols < h, cols + h, cols - h)\n    xs = tl.load(x_ptr + shifted, mask=m, other=0.0).to(tl.float32)\n    ws = tl.load(w_ptr + shifted, mask=m, other=0.0).to(tl.float32)\n    rot = tl.where(cols < h, -1.0, 1.0) * (xs * r * ws)\n    cs = tl.load(cos_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    sn = tl.load(sin_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, n * cs + rot * sn, mask=m)\ndef run(x, w, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, cos, sin, y, x.stride(0), D, 1e-6, BLOCK=triton.next_power_of_2(D), num_warps=4)\n    return y\n", "khash": "0611307bc259c034", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "qknorm_rope", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2; cols = tl.arange(0, BLOCK); m = cols < D\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    r = tl.rsqrt(tl.sum(x * x) / D + eps)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    n = x * r * w\n    shifted = tl.where(cols < h, cols + h, cols - h)\n    xs = tl.load(x_ptr + shifted, mask=m, other=0.0).to(tl.float32)\n    ws = tl.load(w_ptr + shifted, mask=m, other=0.0).to(tl.float32)\n    rot = tl.where(cols < h, -1.0, 1.0) * (xs * r * ws)\n    cs = tl.load(cos_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    sn = tl.load(sin_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, n * cs + rot * sn, mask=m)\ndef run(x, w, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, cos, sin, y, x.stride(0), D, 1e-6, BLOCK=triton.next_power_of_2(D), num_warps=8)\n    return y\n", "khash": "c7d59e062eb646a2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "qknorm_rope", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2; cols = tl.arange(0, BLOCK); m = cols < D\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    r = tl.rsqrt(tl.sum(x * x) / D + eps)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    n = x * r * w\n    shifted = tl.where(cols < h, cols + h, cols - h)\n    xs = tl.load(x_ptr + shifted, mask=m, other=0.0).to(tl.float32)\n    ws = tl.load(w_ptr + shifted, mask=m, other=0.0).to(tl.float32)\n    rot = tl.where(cols < h, -1.0, 1.0) * (xs * r * ws)\n    cs = tl.load(cos_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    sn = tl.load(sin_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, n * cs + rot * sn, mask=m)\ndef run(x, w, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, cos, sin, y, x.stride(0), D, 1e-6, BLOCK=triton.next_power_of_2(D), num_stages=2)\n    return y\n", "khash": "f4499b7c247e676d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "qknorm_rope", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2\n    full = tl.load(x_ptr + tl.arange(0, BLOCK), mask=tl.arange(0, BLOCK) < D, other=0.0).to(tl.float32)\n    r = tl.rsqrt(tl.sum(full * full) / D + eps)\n    c = tl.arange(0, BLOCK); m = c < h\n    x1 = tl.load(x_ptr + c, mask=m, other=0.0).to(tl.float32)\n    x2 = tl.load(x_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    w1 = tl.load(w_ptr + c, mask=m, other=0.0).to(tl.float32)\n    w2 = tl.load(w_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    n1 = x1 * r * w1; n2 = x2 * r * w2\n    c1 = tl.load(cos_ptr + c, mask=m, other=0.0).to(tl.float32)\n    s1 = tl.load(sin_ptr + c, mask=m, other=0.0).to(tl.float32)\n    c2 = tl.load(cos_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    s2 = tl.load(sin_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + c, n1 * c1 - n2 * s1, mask=m)\n    tl.store(y_ptr + h + c, n2 * c2 + n1 * s2, mask=m)\ndef run(x, w, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, cos, sin, y, x.stride(0), D, 1e-6, BLOCK=triton.next_power_of_2(D))\n    return y\n", "khash": "d0cabf3d2743c66d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "qknorm_rope", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2\n    full = tl.load(x_ptr + tl.arange(0, BLOCK), mask=tl.arange(0, BLOCK) < D, other=0.0).to(tl.float32)\n    r = tl.rsqrt(tl.sum(full * full) / D + eps)\n    c = tl.arange(0, BLOCK); m = c < h\n    x1 = tl.load(x_ptr + c, mask=m, other=0.0).to(tl.float32)\n    x2 = tl.load(x_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    w1 = tl.load(w_ptr + c, mask=m, other=0.0).to(tl.float32)\n    w2 = tl.load(w_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    n1 = x1 * r * w1; n2 = x2 * r * w2\n    c1 = tl.load(cos_ptr + c, mask=m, other=0.0).to(tl.float32)\n    s1 = tl.load(sin_ptr + c, mask=m, other=0.0).to(tl.float32)\n    c2 = tl.load(cos_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    s2 = tl.load(sin_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + c, n1 * c1 - n2 * s1, mask=m)\n    tl.store(y_ptr + h + c, n2 * c2 + n1 * s2, mask=m)\ndef run(x, w, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, cos, sin, y, x.stride(0), D, 1e-6, BLOCK=triton.next_power_of_2(D), num_warps=4)\n    return y\n", "khash": "0d474c8aa6ed64d4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "qknorm_rope", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2\n    full = tl.load(x_ptr + tl.arange(0, BLOCK), mask=tl.arange(0, BLOCK) < D, other=0.0).to(tl.float32)\n    r = tl.rsqrt(tl.sum(full * full) / D + eps)\n    c = tl.arange(0, BLOCK); m = c < h\n    x1 = tl.load(x_ptr + c, mask=m, other=0.0).to(tl.float32)\n    x2 = tl.load(x_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    w1 = tl.load(w_ptr + c, mask=m, other=0.0).to(tl.float32)\n    w2 = tl.load(w_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    n1 = x1 * r * w1; n2 = x2 * r * w2\n    c1 = tl.load(cos_ptr + c, mask=m, other=0.0).to(tl.float32)\n    s1 = tl.load(sin_ptr + c, mask=m, other=0.0).to(tl.float32)\n    c2 = tl.load(cos_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    s2 = tl.load(sin_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + c, n1 * c1 - n2 * s1, mask=m)\n    tl.store(y_ptr + h + c, n2 * c2 + n1 * s2, mask=m)\ndef run(x, w, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, cos, sin, y, x.stride(0), D, 1e-6, BLOCK=triton.next_power_of_2(D), num_warps=8)\n    return y\n", "khash": "894f2e8d28f5d172", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "qknorm_rope", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2\n    full = tl.load(x_ptr + tl.arange(0, BLOCK), mask=tl.arange(0, BLOCK) < D, other=0.0).to(tl.float32)\n    r = tl.rsqrt(tl.sum(full * full) / D + eps)\n    c = tl.arange(0, BLOCK); m = c < h\n    x1 = tl.load(x_ptr + c, mask=m, other=0.0).to(tl.float32)\n    x2 = tl.load(x_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    w1 = tl.load(w_ptr + c, mask=m, other=0.0).to(tl.float32)\n    w2 = tl.load(w_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    n1 = x1 * r * w1; n2 = x2 * r * w2\n    c1 = tl.load(cos_ptr + c, mask=m, other=0.0).to(tl.float32)\n    s1 = tl.load(sin_ptr + c, mask=m, other=0.0).to(tl.float32)\n    c2 = tl.load(cos_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    s2 = tl.load(sin_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + c, n1 * c1 - n2 * s1, mask=m)\n    tl.store(y_ptr + h + c, n2 * c2 + n1 * s2, mask=m)\ndef run(x, w, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, cos, sin, y, x.stride(0), D, 1e-6, BLOCK=triton.next_power_of_2(D), num_stages=2)\n    return y\n", "khash": "63a44524e790e316", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "qknorm_rope", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2\n    full = tl.load(x_ptr + tl.arange(0, BLOCK), mask=tl.arange(0, BLOCK) < D, other=0.0).to(tl.float32)\n    r = tl.rsqrt(tl.sum(full * full) / D + eps)\n    c = tl.arange(0, BLOCK); m = c < h\n    x1 = tl.load(x_ptr + c, mask=m, other=0.0).to(tl.float32)\n    x2 = tl.load(x_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    w1 = tl.load(w_ptr + c, mask=m, other=0.0).to(tl.float32)\n    w2 = tl.load(w_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    cc = tl.load(cos_ptr + c, mask=m, other=0.0).to(tl.float32)\n    ss = tl.load(sin_ptr + c, mask=m, other=0.0).to(tl.float32)\n    n1 = x1 * r * w1; n2 = x2 * r * w2\n    tl.store(y_ptr + c, n1 * cc - n2 * ss, mask=m)\n    tl.store(y_ptr + h + c, n2 * cc + n1 * ss, mask=m)\ndef run(x, w, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, cos, sin, y, x.stride(0), D, 1e-6, BLOCK=triton.next_power_of_2(D))\n    return y\n", "khash": "dd1e915894ee41c1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "qknorm_rope", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2\n    full = tl.load(x_ptr + tl.arange(0, BLOCK), mask=tl.arange(0, BLOCK) < D, other=0.0).to(tl.float32)\n    r = tl.rsqrt(tl.sum(full * full) / D + eps)\n    c = tl.arange(0, BLOCK); m = c < h\n    x1 = tl.load(x_ptr + c, mask=m, other=0.0).to(tl.float32)\n    x2 = tl.load(x_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    w1 = tl.load(w_ptr + c, mask=m, other=0.0).to(tl.float32)\n    w2 = tl.load(w_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    cc = tl.load(cos_ptr + c, mask=m, other=0.0).to(tl.float32)\n    ss = tl.load(sin_ptr + c, mask=m, other=0.0).to(tl.float32)\n    n1 = x1 * r * w1; n2 = x2 * r * w2\n    tl.store(y_ptr + c, n1 * cc - n2 * ss, mask=m)\n    tl.store(y_ptr + h + c, n2 * cc + n1 * ss, mask=m)\ndef run(x, w, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, cos, sin, y, x.stride(0), D, 1e-6, BLOCK=triton.next_power_of_2(D), num_warps=4)\n    return y\n", "khash": "e1b5d2260448b8a4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "qknorm_rope", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2\n    full = tl.load(x_ptr + tl.arange(0, BLOCK), mask=tl.arange(0, BLOCK) < D, other=0.0).to(tl.float32)\n    r = tl.rsqrt(tl.sum(full * full) / D + eps)\n    c = tl.arange(0, BLOCK); m = c < h\n    x1 = tl.load(x_ptr + c, mask=m, other=0.0).to(tl.float32)\n    x2 = tl.load(x_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    w1 = tl.load(w_ptr + c, mask=m, other=0.0).to(tl.float32)\n    w2 = tl.load(w_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    cc = tl.load(cos_ptr + c, mask=m, other=0.0).to(tl.float32)\n    ss = tl.load(sin_ptr + c, mask=m, other=0.0).to(tl.float32)\n    n1 = x1 * r * w1; n2 = x2 * r * w2\n    tl.store(y_ptr + c, n1 * cc - n2 * ss, mask=m)\n    tl.store(y_ptr + h + c, n2 * cc + n1 * ss, mask=m)\ndef run(x, w, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, cos, sin, y, x.stride(0), D, 1e-6, BLOCK=triton.next_power_of_2(D), num_warps=8)\n    return y\n", "khash": "dfc67c3ce7f9cf21", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "qknorm_rope", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2\n    full = tl.load(x_ptr + tl.arange(0, BLOCK), mask=tl.arange(0, BLOCK) < D, other=0.0).to(tl.float32)\n    r = tl.rsqrt(tl.sum(full * full) / D + eps)\n    c = tl.arange(0, BLOCK); m = c < h\n    x1 = tl.load(x_ptr + c, mask=m, other=0.0).to(tl.float32)\n    x2 = tl.load(x_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    w1 = tl.load(w_ptr + c, mask=m, other=0.0).to(tl.float32)\n    w2 = tl.load(w_ptr + h + c, mask=m, other=0.0).to(tl.float32)\n    cc = tl.load(cos_ptr + c, mask=m, other=0.0).to(tl.float32)\n    ss = tl.load(sin_ptr + c, mask=m, other=0.0).to(tl.float32)\n    n1 = x1 * r * w1; n2 = x2 * r * w2\n    tl.store(y_ptr + c, n1 * cc - n2 * ss, mask=m)\n    tl.store(y_ptr + h + c, n2 * cc + n1 * ss, mask=m)\ndef run(x, w, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, cos, sin, y, x.stride(0), D, 1e-6, BLOCK=triton.next_power_of_2(D), num_stages=2)\n    return y\n", "khash": "5ccc6c95b449e3aa", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=1024)\n    return y\n", "khash": "ee32ba5603ebb2eb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=2048)\n    return y\n", "khash": "31c157def9190dc7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=4096)\n    return y\n", "khash": "8e00ca02baad0c53", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "7d5c78752e28c404", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "bb7bbc44fa8b9fd5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "7a37aba1038e1e48", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "b05876231bb2f58f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "1c50731b092aa422", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "e6a787fd3d446ae9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "32d007492afe02ac", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "438fb6d8b3cd8bd3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "f5fdb2b1a6f3cc90", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=1024)\n    return y\n", "khash": "440625f5b48f6c5f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=256)\n    return y\n", "khash": "395de0a6caf3e48e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=512)\n    return y\n", "khash": "2837f5f42ed1027d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=2048)\n    return y\n", "khash": "7c2e5a1715e66d60", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=4096)\n    return y\n", "khash": "d3a65db3636f3e7b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "4fff63812add5ce3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "7ed8f1a4035c64ab", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "78071e2cf4a168e3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=256, num_warps=4)\n    return y\n", "khash": "117689e1e6acc45a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=256, num_warps=8)\n    return y\n", "khash": "06ecf9759ad01c3c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=256, num_stages=2)\n    return y\n", "khash": "867a03c56184167e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=512, num_warps=4)\n    return y\n", "khash": "98cfb46f26e8a373", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=512, num_warps=8)\n    return y\n", "khash": "a19e9292318df53c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=512, num_stages=2)\n    return y\n", "khash": "6c8f6d4dec70a659", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "9c1c5ec057c29c84", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "7ec24fe481ae75d3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "f9c0b3b19984b669", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "adeb98e4b8ead967", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "4faad7108a37d89e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "reglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(g, 0.0) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "a410a56e975b331e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=1024)\n    return y\n", "khash": "9359c18e5e32f23d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=2048)\n    return y\n", "khash": "d9e0ad400e44aeec", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=4096)\n    return y\n", "khash": "d8c85532f5cf7209", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "e9497d48c31d9776", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "4f77680480debe6d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "c7599e5e9f964193", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "fec28edec91cdb74", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "9665bacb4c0855ab", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "fcb1144b93c6c779", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "d46096c413f35151", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "0b07b23acaeed7d1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "a5bfb6679e5a4023", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024)\n    return y\n", "khash": "88dd58fe1c357891", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256)\n    return y\n", "khash": "c16fd8a55cb06cec", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512)\n    return y\n", "khash": "bfc2a0ae0e3b3283", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048)\n    return y\n", "khash": "45e39ff6910e5a83", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096)\n    return y\n", "khash": "4b5c23b57c822f36", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "20a14ba6b9335cfe", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "0ffc47844e7ce0e4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "1af71e46852fba84", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256, num_warps=4)\n    return y\n", "khash": "4710f4bb39315566", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256, num_warps=8)\n    return y\n", "khash": "56a00a31c84280b8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256, num_stages=2)\n    return y\n", "khash": "3ab635acd59b0276", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512, num_warps=4)\n    return y\n", "khash": "abef9958889c1714", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512, num_warps=8)\n    return y\n", "khash": "faa9bfa4b74367e3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512, num_stages=2)\n    return y\n", "khash": "f7823c7d92628c22", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "afe2ad013c39385b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "ddee25bde7e64c0e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "5952a47578f5472a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "e43383dc8f6403db", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "191901d7b8a3d68c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "relu2", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.maximum(x, 0.0) * tl.maximum(x, 0.0), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "3d4f8422d0f33833", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        acc += x * x\n    r = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=1024)\n    return y\n", "khash": "51c1459433f557bf", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        acc += x * x\n    r = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=256)\n    return y\n", "khash": "a35c34d1ab05b3d7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        acc += x * x\n    r = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=512)\n    return y\n", "khash": "8b20a6cffa61973b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        acc += x * x\n    r = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=2048)\n    return y\n", "khash": "c3c9d11e654fe749", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        acc += x * x\n    r = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=4096)\n    return y\n", "khash": "ecce7d2ace7485c2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        acc += x * x\n    r = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "9af6dad4cf4ea857", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        acc += x * x\n    r = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "224a3f14605c42ea", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        acc += x * x\n    r = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "b050009939858d24", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        acc += x * x\n    r = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=256, num_warps=4)\n    return y\n", "khash": "ffdf0ee397eeec62", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        acc += x * x\n    r = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=256, num_warps=8)\n    return y\n", "khash": "2ecf94960d60e5fc", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        acc += x * x\n    r = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=256, num_stages=2)\n    return y\n", "khash": "847afc6be3583e3d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        acc += x * x\n    r = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=512, num_warps=4)\n    return y\n", "khash": "58588c10d57a8897", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        acc += x * x\n    r = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=512, num_warps=8)\n    return y\n", "khash": "a9750a435add0372", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        acc += x * x\n    r = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=512, num_stages=2)\n    return y\n", "khash": "9d9fd58bb6c228cb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        acc += x * x\n    r = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "bc2ceac43fa65fbd", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        acc += x * x\n    r = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "10b5a672ca145ab6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        acc += x * x\n    r = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "4702a91eb5c4e452", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        acc += x * x\n    r = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "69cc12ad420992c1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        acc += x * x\n    r = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "d8f950b6eed6de80", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    acc = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        acc += x * x\n    r = tl.rsqrt(tl.sum(acc) / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "d44948b727d37b45", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        s += tl.sum(x * x)\n    r = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=1024)\n    return y\n", "khash": "5a5c93eb03d53a45", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        s += tl.sum(x * x)\n    r = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=256)\n    return y\n", "khash": "e1520f37347dc56a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        s += tl.sum(x * x)\n    r = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=512)\n    return y\n", "khash": "437979ddf0009c96", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        s += tl.sum(x * x)\n    r = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=2048)\n    return y\n", "khash": "b6ab0c567e4eb840", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        s += tl.sum(x * x)\n    r = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=4096)\n    return y\n", "khash": "5aa3e37d33cbddac", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        s += tl.sum(x * x)\n    r = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "b17c8a7bc8e75f97", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        s += tl.sum(x * x)\n    r = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "cef00b9aa44bda5c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        s += tl.sum(x * x)\n    r = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "5d28e657c0e84339", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        s += tl.sum(x * x)\n    r = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=256, num_warps=4)\n    return y\n", "khash": "1b950fa8d2537ec7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        s += tl.sum(x * x)\n    r = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=256, num_warps=8)\n    return y\n", "khash": "6e6890cd8a2569bc", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        s += tl.sum(x * x)\n    r = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=256, num_stages=2)\n    return y\n", "khash": "3b161a85a2b7006b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        s += tl.sum(x * x)\n    r = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=512, num_warps=4)\n    return y\n", "khash": "696f6ffa6b960318", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        s += tl.sum(x * x)\n    r = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=512, num_warps=8)\n    return y\n", "khash": "eb3db6341924b008", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        s += tl.sum(x * x)\n    r = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=512, num_stages=2)\n    return y\n", "khash": "d16e07c3cb9680f7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        s += tl.sum(x * x)\n    r = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "b90ceae87e05014a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        s += tl.sum(x * x)\n    r = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "ba1e36c6b8274666", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        s += tl.sum(x * x)\n    r = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "cbb4f7f75df1af47", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        s += tl.sum(x * x)\n    r = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "7d17ec86ed7ecfae", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        s += tl.sum(x * x)\n    r = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "90d9f5fadc04af3c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=0.0).to(tl.float32)\n        s += tl.sum(x * x)\n    r = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "1fdcffec6026221b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + row * stride + cols, mask=m, other=0.0).to(tl.float32)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    r = tl.rsqrt(tl.sum(x * x) / N + eps)\n    tl.store(y_ptr + row * stride + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "03f58e9e76921e2c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + row * stride + cols, mask=m, other=0.0).to(tl.float32)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    r = tl.rsqrt(tl.sum(x * x) / N + eps)\n    tl.store(y_ptr + row * stride + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "534ac90dc3c541fb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + row * stride + cols, mask=m, other=0.0).to(tl.float32)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    r = tl.rsqrt(tl.sum(x * x) / N + eps)\n    tl.store(y_ptr + row * stride + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "f25e4bcb685b50b5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm", "src": "\n@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + row * stride + cols, mask=m, other=0.0).to(tl.float32)\n    w = tl.load(w_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    r = tl.rsqrt(tl.sum(x * x) / N + eps)\n    tl.store(y_ptr + row * stride + cols, x * r * w, mask=m)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-6, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "f0e4e17fe83284de", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024)\n    return y\n", "khash": "44e97af79e7ab7ff", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256)\n    return y\n", "khash": "babb64b88d160dc3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512)\n    return y\n", "khash": "acf44e38bc74691e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048)\n    return y\n", "khash": "639048f64769cb88", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096)\n    return y\n", "khash": "83ed96dd04f111c8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "319e4ea98c1aaed1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "91ed47e6eb795383", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "6af750f9ed6b8534", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=4)\n    return y\n", "khash": "6c82593a6cc9d7e0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=8)\n    return y\n", "khash": "6f1b70ba7e0a6d88", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_stages=2)\n    return y\n", "khash": "1c07dd7765df2721", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=4)\n    return y\n", "khash": "4997259fe87fe820", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=8)\n    return y\n", "khash": "7f411a8896b0fe58", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_stages=2)\n    return y\n", "khash": "5e9590087bc18c0b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "30d4413904ead3fb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "6b4ce90925368422", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "43e2ddb2ae3f083e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "d966d5f5c5c86fc8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "4deb6a1f47523629", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "59d1a0b4ba979465", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "8a20e3c0a4cd9626", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "6330769f64e7e76a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "24939c27b9151992", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_gelu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (0.5 * n * (1.0 + (2.0 * tl.sigmoid(2.0 * (0.7978845608028654 * (n + 0.044715 * n * n * n))) - 1.0))), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "9207a5249d644ec9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024)\n    return y\n", "khash": "87f27d50a1e6f72a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256)\n    return y\n", "khash": "8e7b1ef52051cb7c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512)\n    return y\n", "khash": "69229f2bddd86557", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048)\n    return y\n", "khash": "78aa62cf23e7482c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096)\n    return y\n", "khash": "e33f4acbef51a2ba", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "0d555fcd0b1732e1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "022ce128118372aa", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "4ab2fc3d5caf8c89", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=4)\n    return y\n", "khash": "847ef0a1ef1c867d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=8)\n    return y\n", "khash": "17d87116a0cb25d9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_stages=2)\n    return y\n", "khash": "fb0f97b5c06ce708", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=4)\n    return y\n", "khash": "8e6eedae87f50201", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=8)\n    return y\n", "khash": "8bb4052eac029659", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_stages=2)\n    return y\n", "khash": "e5b0c7ce2eba7cf9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "3cadab400dca9ff7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "5cd24a05a9ca1ff8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "e9f77dbc0839be3d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "1fdcd227097c6071", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "63c2d51fbe19d95a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "29201a2cbf17d711", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "22f26c26b29791bc", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "4742b5479d7fb531", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "ccaf4c2596a71ead", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu2", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (tl.maximum(n, 0.0) * tl.maximum(n, 0.0)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "65b151b156e971cf", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024)\n    return y\n", "khash": "c5aecbff340bd18a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256)\n    return y\n", "khash": "5ac829bc390e2753", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512)\n    return y\n", "khash": "9e4dccfe8a617fdb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048)\n    return y\n", "khash": "2b525bbb92a3d6b9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096)\n    return y\n", "khash": "21841066e13f42b1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "bc3d31667d4d4b00", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "c16bc0c7a99d4826", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "83320640a8248d4f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=4)\n    return y\n", "khash": "52b28265f24cbeaf", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=8)\n    return y\n", "khash": "2a6b3c886c855fc1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_stages=2)\n    return y\n", "khash": "259f1b2839251676", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=4)\n    return y\n", "khash": "9f1d5958488c4cf8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=8)\n    return y\n", "khash": "c883aa08d47df45d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_stages=2)\n    return y\n", "khash": "95059a5152b56ec3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "a5d1974b8bab51ff", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "5a532e1d87984f8a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "28b9f0b68e01cf75", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "aeec36f0baf1b456", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "5a4f3a73f452b977", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "05051238e10bd360", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "56e597cf22db7d39", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "8e814303b1561570", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "1fb59a13d559c367", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_silu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (n * tl.sigmoid(n)), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "35ce62d332e7802a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rope", "src": "\n@triton.jit\ndef _k(x_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2; cols = tl.arange(0, BLOCK); m = cols < h\n    x1 = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    x2 = tl.load(x_ptr + h + cols, mask=m, other=0.0).to(tl.float32)\n    c1 = tl.load(cos_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    s1 = tl.load(sin_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    c2 = tl.load(cos_ptr + h + cols, mask=m, other=0.0).to(tl.float32)\n    s2 = tl.load(sin_ptr + h + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x1 * c1 - x2 * s1, mask=m)\n    tl.store(y_ptr + h + cols, x2 * c2 + x1 * s2, mask=m)\ndef run(x, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, cos, sin, y, x.stride(0), D, BLOCK=triton.next_power_of_2(D // 2))\n    return y\n", "khash": "0cb28a07077e5f7f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rope", "src": "\n@triton.jit\ndef _k(x_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2; cols = tl.arange(0, BLOCK); m = cols < h\n    x1 = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    x2 = tl.load(x_ptr + h + cols, mask=m, other=0.0).to(tl.float32)\n    c1 = tl.load(cos_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    s1 = tl.load(sin_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    c2 = tl.load(cos_ptr + h + cols, mask=m, other=0.0).to(tl.float32)\n    s2 = tl.load(sin_ptr + h + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x1 * c1 - x2 * s1, mask=m)\n    tl.store(y_ptr + h + cols, x2 * c2 + x1 * s2, mask=m)\ndef run(x, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, cos, sin, y, x.stride(0), D, BLOCK=triton.next_power_of_2(D // 2), num_warps=4)\n    return y\n", "khash": "02c3f2beda57e14b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rope", "src": "\n@triton.jit\ndef _k(x_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2; cols = tl.arange(0, BLOCK); m = cols < h\n    x1 = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    x2 = tl.load(x_ptr + h + cols, mask=m, other=0.0).to(tl.float32)\n    c1 = tl.load(cos_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    s1 = tl.load(sin_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    c2 = tl.load(cos_ptr + h + cols, mask=m, other=0.0).to(tl.float32)\n    s2 = tl.load(sin_ptr + h + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x1 * c1 - x2 * s1, mask=m)\n    tl.store(y_ptr + h + cols, x2 * c2 + x1 * s2, mask=m)\ndef run(x, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, cos, sin, y, x.stride(0), D, BLOCK=triton.next_power_of_2(D // 2), num_warps=8)\n    return y\n", "khash": "8a0bdde69618641a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rope", "src": "\n@triton.jit\ndef _k(x_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2; cols = tl.arange(0, BLOCK); m = cols < h\n    x1 = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    x2 = tl.load(x_ptr + h + cols, mask=m, other=0.0).to(tl.float32)\n    c1 = tl.load(cos_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    s1 = tl.load(sin_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    c2 = tl.load(cos_ptr + h + cols, mask=m, other=0.0).to(tl.float32)\n    s2 = tl.load(sin_ptr + h + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x1 * c1 - x2 * s1, mask=m)\n    tl.store(y_ptr + h + cols, x2 * c2 + x1 * s2, mask=m)\ndef run(x, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, cos, sin, y, x.stride(0), D, BLOCK=triton.next_power_of_2(D // 2), num_stages=2)\n    return y\n", "khash": "4fdd653071c93595", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rope", "src": "\n@triton.jit\ndef _k(x_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2; cols = tl.arange(0, BLOCK); m = cols < D\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    cs = tl.load(cos_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    sn = tl.load(sin_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    shifted = tl.where(cols < h, cols + h, cols - h)\n    xr = tl.load(x_ptr + shifted, mask=m, other=0.0).to(tl.float32)\n    sign = tl.where(cols < h, -1.0, 1.0)\n    tl.store(y_ptr + cols, x * cs + sign * xr * sn, mask=m)\ndef run(x, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, cos, sin, y, x.stride(0), D, BLOCK=triton.next_power_of_2(D))\n    return y\n", "khash": "37c7ddb062250745", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rope", "src": "\n@triton.jit\ndef _k(x_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2; cols = tl.arange(0, BLOCK); m = cols < D\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    cs = tl.load(cos_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    sn = tl.load(sin_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    shifted = tl.where(cols < h, cols + h, cols - h)\n    xr = tl.load(x_ptr + shifted, mask=m, other=0.0).to(tl.float32)\n    sign = tl.where(cols < h, -1.0, 1.0)\n    tl.store(y_ptr + cols, x * cs + sign * xr * sn, mask=m)\ndef run(x, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, cos, sin, y, x.stride(0), D, BLOCK=triton.next_power_of_2(D), num_warps=4)\n    return y\n", "khash": "6fa79bd1e3c2ad58", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rope", "src": "\n@triton.jit\ndef _k(x_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2; cols = tl.arange(0, BLOCK); m = cols < D\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    cs = tl.load(cos_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    sn = tl.load(sin_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    shifted = tl.where(cols < h, cols + h, cols - h)\n    xr = tl.load(x_ptr + shifted, mask=m, other=0.0).to(tl.float32)\n    sign = tl.where(cols < h, -1.0, 1.0)\n    tl.store(y_ptr + cols, x * cs + sign * xr * sn, mask=m)\ndef run(x, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, cos, sin, y, x.stride(0), D, BLOCK=triton.next_power_of_2(D), num_warps=8)\n    return y\n", "khash": "1ef3d070af5c544d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rope", "src": "\n@triton.jit\ndef _k(x_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2; cols = tl.arange(0, BLOCK); m = cols < D\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    cs = tl.load(cos_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    sn = tl.load(sin_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    shifted = tl.where(cols < h, cols + h, cols - h)\n    xr = tl.load(x_ptr + shifted, mask=m, other=0.0).to(tl.float32)\n    sign = tl.where(cols < h, -1.0, 1.0)\n    tl.store(y_ptr + cols, x * cs + sign * xr * sn, mask=m)\ndef run(x, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, cos, sin, y, x.stride(0), D, BLOCK=triton.next_power_of_2(D), num_stages=2)\n    return y\n", "khash": "c9367b1a84b2e58c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rope", "src": "\n@triton.jit\ndef _k(x_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2; cols = tl.arange(0, BLOCK); m = cols < h\n    x1 = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    x2 = tl.load(x_ptr + h + cols, mask=m, other=0.0).to(tl.float32)\n    c = tl.load(cos_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    s = tl.load(sin_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x1 * c - x2 * s, mask=m)\n    tl.store(y_ptr + h + cols, x2 * c + x1 * s, mask=m)\ndef run(x, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, cos, sin, y, x.stride(0), D, BLOCK=triton.next_power_of_2(D // 2))\n    return y\n", "khash": "be139d1e1e8966cb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rope", "src": "\n@triton.jit\ndef _k(x_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2; cols = tl.arange(0, BLOCK); m = cols < h\n    x1 = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    x2 = tl.load(x_ptr + h + cols, mask=m, other=0.0).to(tl.float32)\n    c = tl.load(cos_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    s = tl.load(sin_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x1 * c - x2 * s, mask=m)\n    tl.store(y_ptr + h + cols, x2 * c + x1 * s, mask=m)\ndef run(x, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, cos, sin, y, x.stride(0), D, BLOCK=triton.next_power_of_2(D // 2), num_warps=4)\n    return y\n", "khash": "888cf85bab2b7099", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rope", "src": "\n@triton.jit\ndef _k(x_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2; cols = tl.arange(0, BLOCK); m = cols < h\n    x1 = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    x2 = tl.load(x_ptr + h + cols, mask=m, other=0.0).to(tl.float32)\n    c = tl.load(cos_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    s = tl.load(sin_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x1 * c - x2 * s, mask=m)\n    tl.store(y_ptr + h + cols, x2 * c + x1 * s, mask=m)\ndef run(x, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, cos, sin, y, x.stride(0), D, BLOCK=triton.next_power_of_2(D // 2), num_warps=8)\n    return y\n", "khash": "c5fc5e358c84ca34", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rope", "src": "\n@triton.jit\ndef _k(x_ptr, cos_ptr, sin_ptr, y_ptr, stride, D, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    x_ptr += row * stride; cos_ptr += row * stride; sin_ptr += row * stride; y_ptr += row * stride\n    h = D // 2; cols = tl.arange(0, BLOCK); m = cols < h\n    x1 = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    x2 = tl.load(x_ptr + h + cols, mask=m, other=0.0).to(tl.float32)\n    c = tl.load(cos_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    s = tl.load(sin_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x1 * c - x2 * s, mask=m)\n    tl.store(y_ptr + h + cols, x2 * c + x1 * s, mask=m)\ndef run(x, cos, sin):\n    M, D = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, cos, sin, y, x.stride(0), D, BLOCK=triton.next_power_of_2(D // 2), num_stages=2)\n    return y\n", "khash": "a7d5d5a51b5694c2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=1024)\n    return y\n", "khash": "949b2e8502a9b60a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=2048)\n    return y\n", "khash": "30cfe40623d9fe78", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=4096)\n    return y\n", "khash": "ae0d5b0c5d8125fc", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "15ff8a7fc884b7a2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "094626715e8defaf", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "2c0affbc6f929c83", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "3ecfd34d30c91fb3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "cebff9ae68433930", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "c802f05d6daad834", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "d66182a29053a640", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "6765f3c8c2e9557d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    y = torch.empty_like(x); n = x.numel()\n    _k[(triton.cdiv(n, 1024),)](x, y, n, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "9f59603c6ff90ed1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3: tl.constexpr):\n    v4 = tl.program_id(v5) * v"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024)\n    return y\n", "khash": "c0fc83ab40a9657e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256)\n    return y\n", "khash": "005783039df80022", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512)\n    return y\n", "khash": "65107e4dd6c29666", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048)\n    return y\n", "khash": "c90d46f995946d4d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096)\n    return y\n", "khash": "47b1b2fdd82a194e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "6a29e3941b6af7c9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "956f2458ac92e5fb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "3a5a560cd68511ba", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256, num_warps=4)\n    return y\n", "khash": "b53a43da56a921b9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256, num_warps=8)\n    return y\n", "khash": "2512e705e2fdcfd0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256, num_stages=2)\n    return y\n", "khash": "07adb79ce667d8ce", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512, num_warps=4)\n    return y\n", "khash": "7c61ef3515492f91", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512, num_warps=8)\n    return y\n", "khash": "c8ed2997cb923c9e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512, num_stages=2)\n    return y\n", "khash": "dc82dd4d30c422fb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "7d467875667bfcca", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "56aeb2f58d797cac", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "aefc39f653690fdb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "ef2ead97f2a0b65c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "d6bd62852d874273", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "silu", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, x * tl.sigmoid(x), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "7e68fa10a6dcb587", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = tl.full([BLOCK], -float(\"inf\"), dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        mx = tl.maximum(mx, x)\n    rmax = tl.max(mx)\n    d = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        d += tl.where(cols < N, tl.exp(x - rmax), 0.0)\n    den = tl.sum(d)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - rmax) / den, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024)\n    return y\n", "khash": "98cf9fbe3fa2370e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = tl.full([BLOCK], -float(\"inf\"), dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        mx = tl.maximum(mx, x)\n    rmax = tl.max(mx)\n    d = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        d += tl.where(cols < N, tl.exp(x - rmax), 0.0)\n    den = tl.sum(d)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - rmax) / den, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256)\n    return y\n", "khash": "0761d51d96e74267", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = tl.full([BLOCK], -float(\"inf\"), dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        mx = tl.maximum(mx, x)\n    rmax = tl.max(mx)\n    d = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        d += tl.where(cols < N, tl.exp(x - rmax), 0.0)\n    den = tl.sum(d)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - rmax) / den, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512)\n    return y\n", "khash": "31eb60ad4d85e712", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = tl.full([BLOCK], -float(\"inf\"), dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        mx = tl.maximum(mx, x)\n    rmax = tl.max(mx)\n    d = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        d += tl.where(cols < N, tl.exp(x - rmax), 0.0)\n    den = tl.sum(d)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - rmax) / den, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048)\n    return y\n", "khash": "f26bb638a408852e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = tl.full([BLOCK], -float(\"inf\"), dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        mx = tl.maximum(mx, x)\n    rmax = tl.max(mx)\n    d = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        d += tl.where(cols < N, tl.exp(x - rmax), 0.0)\n    den = tl.sum(d)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - rmax) / den, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096)\n    return y\n", "khash": "dedae483c6a904f6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = tl.full([BLOCK], -float(\"inf\"), dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        mx = tl.maximum(mx, x)\n    rmax = tl.max(mx)\n    d = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        d += tl.where(cols < N, tl.exp(x - rmax), 0.0)\n    den = tl.sum(d)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - rmax) / den, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "e3a651d2100861f7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = tl.full([BLOCK], -float(\"inf\"), dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        mx = tl.maximum(mx, x)\n    rmax = tl.max(mx)\n    d = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        d += tl.where(cols < N, tl.exp(x - rmax), 0.0)\n    den = tl.sum(d)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - rmax) / den, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "da737713c0416890", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = tl.full([BLOCK], -float(\"inf\"), dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        mx = tl.maximum(mx, x)\n    rmax = tl.max(mx)\n    d = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        d += tl.where(cols < N, tl.exp(x - rmax), 0.0)\n    den = tl.sum(d)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - rmax) / den, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "bcc06fb59eee817c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = tl.full([BLOCK], -float(\"inf\"), dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        mx = tl.maximum(mx, x)\n    rmax = tl.max(mx)\n    d = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        d += tl.where(cols < N, tl.exp(x - rmax), 0.0)\n    den = tl.sum(d)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - rmax) / den, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256, num_warps=4)\n    return y\n", "khash": "4b1c8a4130a66545", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = tl.full([BLOCK], -float(\"inf\"), dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        mx = tl.maximum(mx, x)\n    rmax = tl.max(mx)\n    d = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        d += tl.where(cols < N, tl.exp(x - rmax), 0.0)\n    den = tl.sum(d)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - rmax) / den, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256, num_warps=8)\n    return y\n", "khash": "d327138eff282403", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = tl.full([BLOCK], -float(\"inf\"), dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        mx = tl.maximum(mx, x)\n    rmax = tl.max(mx)\n    d = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        d += tl.where(cols < N, tl.exp(x - rmax), 0.0)\n    den = tl.sum(d)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - rmax) / den, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256, num_stages=2)\n    return y\n", "khash": "140263aead965bfe", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = tl.full([BLOCK], -float(\"inf\"), dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        mx = tl.maximum(mx, x)\n    rmax = tl.max(mx)\n    d = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        d += tl.where(cols < N, tl.exp(x - rmax), 0.0)\n    den = tl.sum(d)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - rmax) / den, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512, num_warps=4)\n    return y\n", "khash": "c8e9bfedd273b807", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = tl.full([BLOCK], -float(\"inf\"), dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        mx = tl.maximum(mx, x)\n    rmax = tl.max(mx)\n    d = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        d += tl.where(cols < N, tl.exp(x - rmax), 0.0)\n    den = tl.sum(d)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - rmax) / den, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512, num_warps=8)\n    return y\n", "khash": "3fdea6416a76d018", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = tl.full([BLOCK], -float(\"inf\"), dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        mx = tl.maximum(mx, x)\n    rmax = tl.max(mx)\n    d = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        d += tl.where(cols < N, tl.exp(x - rmax), 0.0)\n    den = tl.sum(d)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - rmax) / den, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512, num_stages=2)\n    return y\n", "khash": "246f387cc5161c71", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = tl.full([BLOCK], -float(\"inf\"), dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        mx = tl.maximum(mx, x)\n    rmax = tl.max(mx)\n    d = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        d += tl.where(cols < N, tl.exp(x - rmax), 0.0)\n    den = tl.sum(d)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - rmax) / den, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "2d9ce4a13a2b0c27", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = tl.full([BLOCK], -float(\"inf\"), dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        mx = tl.maximum(mx, x)\n    rmax = tl.max(mx)\n    d = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        d += tl.where(cols < N, tl.exp(x - rmax), 0.0)\n    den = tl.sum(d)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - rmax) / den, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "1864b84cb9127173", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = tl.full([BLOCK], -float(\"inf\"), dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        mx = tl.maximum(mx, x)\n    rmax = tl.max(mx)\n    d = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        d += tl.where(cols < N, tl.exp(x - rmax), 0.0)\n    den = tl.sum(d)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - rmax) / den, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "a847bbc576fd058e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = tl.full([BLOCK], -float(\"inf\"), dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        mx = tl.maximum(mx, x)\n    rmax = tl.max(mx)\n    d = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        d += tl.where(cols < N, tl.exp(x - rmax), 0.0)\n    den = tl.sum(d)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - rmax) / den, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "4351dbe943acc7b1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = tl.full([BLOCK], -float(\"inf\"), dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        mx = tl.maximum(mx, x)\n    rmax = tl.max(mx)\n    d = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        d += tl.where(cols < N, tl.exp(x - rmax), 0.0)\n    den = tl.sum(d)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - rmax) / den, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "9e905377ddf90fc7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    mx = tl.full([BLOCK], -float(\"inf\"), dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        mx = tl.maximum(mx, x)\n    rmax = tl.max(mx)\n    d = tl.zeros([BLOCK], dtype=tl.float32)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        d += tl.where(cols < N, tl.exp(x - rmax), 0.0)\n    den = tl.sum(d)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - rmax) / den, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "12faaa05402cea60", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + row * stride + cols, mask=m, other=-float(\"inf\")).to(tl.float32)\n    x = x - tl.max(x)\n    e = tl.where(m, tl.exp(x), 0.0)\n    tl.store(y_ptr + row * stride + cols, e / tl.sum(e), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "37ff614ee75b6840", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + row * stride + cols, mask=m, other=-float(\"inf\")).to(tl.float32)\n    x = x - tl.max(x)\n    e = tl.where(m, tl.exp(x), 0.0)\n    tl.store(y_ptr + row * stride + cols, e / tl.sum(e), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "8e79266053270033", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + row * stride + cols, mask=m, other=-float(\"inf\")).to(tl.float32)\n    x = x - tl.max(x)\n    e = tl.where(m, tl.exp(x), 0.0)\n    tl.store(y_ptr + row * stride + cols, e / tl.sum(e), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "c037c9531f07f6cf", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0)\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + row * stride + cols, mask=m, other=-float(\"inf\")).to(tl.float32)\n    x = x - tl.max(x)\n    e = tl.where(m, tl.exp(x), 0.0)\n    tl.store(y_ptr + row * stride + cols, e / tl.sum(e), mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "85f47fe34c086817", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    m_i = -float(\"inf\"); l_i = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        m_new = tl.maximum(m_i, tl.max(x))\n        l_i = l_i * tl.exp(m_i - m_new) + tl.sum(tl.where(cols < N, tl.exp(x - m_new), 0.0))\n        m_i = m_new\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - m_i) / l_i, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024)\n    return y\n", "khash": "5cbb35596e47eb4b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    m_i = -float(\"inf\"); l_i = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        m_new = tl.maximum(m_i, tl.max(x))\n        l_i = l_i * tl.exp(m_i - m_new) + tl.sum(tl.where(cols < N, tl.exp(x - m_new), 0.0))\n        m_i = m_new\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - m_i) / l_i, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256)\n    return y\n", "khash": "da543da2b0dd5485", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    m_i = -float(\"inf\"); l_i = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        m_new = tl.maximum(m_i, tl.max(x))\n        l_i = l_i * tl.exp(m_i - m_new) + tl.sum(tl.where(cols < N, tl.exp(x - m_new), 0.0))\n        m_i = m_new\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - m_i) / l_i, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512)\n    return y\n", "khash": "9d1c1b72a1fdfba7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    m_i = -float(\"inf\"); l_i = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        m_new = tl.maximum(m_i, tl.max(x))\n        l_i = l_i * tl.exp(m_i - m_new) + tl.sum(tl.where(cols < N, tl.exp(x - m_new), 0.0))\n        m_i = m_new\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - m_i) / l_i, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048)\n    return y\n", "khash": "907438e4fefa6f68", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    m_i = -float(\"inf\"); l_i = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        m_new = tl.maximum(m_i, tl.max(x))\n        l_i = l_i * tl.exp(m_i - m_new) + tl.sum(tl.where(cols < N, tl.exp(x - m_new), 0.0))\n        m_i = m_new\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - m_i) / l_i, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096)\n    return y\n", "khash": "5480d26e5b656489", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    m_i = -float(\"inf\"); l_i = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        m_new = tl.maximum(m_i, tl.max(x))\n        l_i = l_i * tl.exp(m_i - m_new) + tl.sum(tl.where(cols < N, tl.exp(x - m_new), 0.0))\n        m_i = m_new\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - m_i) / l_i, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "7c3432dbedae8c7d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    m_i = -float(\"inf\"); l_i = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        m_new = tl.maximum(m_i, tl.max(x))\n        l_i = l_i * tl.exp(m_i - m_new) + tl.sum(tl.where(cols < N, tl.exp(x - m_new), 0.0))\n        m_i = m_new\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - m_i) / l_i, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "b6866093a596ed65", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    m_i = -float(\"inf\"); l_i = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        m_new = tl.maximum(m_i, tl.max(x))\n        l_i = l_i * tl.exp(m_i - m_new) + tl.sum(tl.where(cols < N, tl.exp(x - m_new), 0.0))\n        m_i = m_new\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - m_i) / l_i, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "5ffef8262d7045df", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    m_i = -float(\"inf\"); l_i = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        m_new = tl.maximum(m_i, tl.max(x))\n        l_i = l_i * tl.exp(m_i - m_new) + tl.sum(tl.where(cols < N, tl.exp(x - m_new), 0.0))\n        m_i = m_new\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - m_i) / l_i, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256, num_warps=4)\n    return y\n", "khash": "aa5d6cce45ee1774", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    m_i = -float(\"inf\"); l_i = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        m_new = tl.maximum(m_i, tl.max(x))\n        l_i = l_i * tl.exp(m_i - m_new) + tl.sum(tl.where(cols < N, tl.exp(x - m_new), 0.0))\n        m_i = m_new\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - m_i) / l_i, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256, num_warps=8)\n    return y\n", "khash": "77c618b10072c9ea", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    m_i = -float(\"inf\"); l_i = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        m_new = tl.maximum(m_i, tl.max(x))\n        l_i = l_i * tl.exp(m_i - m_new) + tl.sum(tl.where(cols < N, tl.exp(x - m_new), 0.0))\n        m_i = m_new\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - m_i) / l_i, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=256, num_stages=2)\n    return y\n", "khash": "7d9c0671c0429580", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    m_i = -float(\"inf\"); l_i = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        m_new = tl.maximum(m_i, tl.max(x))\n        l_i = l_i * tl.exp(m_i - m_new) + tl.sum(tl.where(cols < N, tl.exp(x - m_new), 0.0))\n        m_i = m_new\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - m_i) / l_i, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512, num_warps=4)\n    return y\n", "khash": "9d923cd19f68e841", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    m_i = -float(\"inf\"); l_i = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        m_new = tl.maximum(m_i, tl.max(x))\n        l_i = l_i * tl.exp(m_i - m_new) + tl.sum(tl.where(cols < N, tl.exp(x - m_new), 0.0))\n        m_i = m_new\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - m_i) / l_i, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512, num_warps=8)\n    return y\n", "khash": "3081d7bd4fedd782", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    m_i = -float(\"inf\"); l_i = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        m_new = tl.maximum(m_i, tl.max(x))\n        l_i = l_i * tl.exp(m_i - m_new) + tl.sum(tl.where(cols < N, tl.exp(x - m_new), 0.0))\n        m_i = m_new\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - m_i) / l_i, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=512, num_stages=2)\n    return y\n", "khash": "1cf1ba9b6b0a4c07", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    m_i = -float(\"inf\"); l_i = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        m_new = tl.maximum(m_i, tl.max(x))\n        l_i = l_i * tl.exp(m_i - m_new) + tl.sum(tl.where(cols < N, tl.exp(x - m_new), 0.0))\n        m_i = m_new\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - m_i) / l_i, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "a2cad5d547fdc373", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    m_i = -float(\"inf\"); l_i = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        m_new = tl.maximum(m_i, tl.max(x))\n        l_i = l_i * tl.exp(m_i - m_new) + tl.sum(tl.where(cols < N, tl.exp(x - m_new), 0.0))\n        m_i = m_new\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - m_i) / l_i, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "a13c86f56d0c1703", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    m_i = -float(\"inf\"); l_i = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        m_new = tl.maximum(m_i, tl.max(x))\n        l_i = l_i * tl.exp(m_i - m_new) + tl.sum(tl.where(cols < N, tl.exp(x - m_new), 0.0))\n        m_i = m_new\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - m_i) / l_i, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "44bbc2433bd093fd", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    m_i = -float(\"inf\"); l_i = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        m_new = tl.maximum(m_i, tl.max(x))\n        l_i = l_i * tl.exp(m_i - m_new) + tl.sum(tl.where(cols < N, tl.exp(x - m_new), 0.0))\n        m_i = m_new\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - m_i) / l_i, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "6b4719ea4082de00", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    m_i = -float(\"inf\"); l_i = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        m_new = tl.maximum(m_i, tl.max(x))\n        l_i = l_i * tl.exp(m_i - m_new) + tl.sum(tl.where(cols < N, tl.exp(x - m_new), 0.0))\n        m_i = m_new\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - m_i) / l_i, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "fafc5988a17087aa", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax", "src": "\n@triton.jit\ndef _k(x_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    m_i = -float(\"inf\"); l_i = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        x = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32)\n        m_new = tl.maximum(m_i, tl.max(x))\n        l_i = l_i * tl.exp(m_i - m_new) + tl.sum(tl.where(cols < N, tl.exp(x - m_new), 0.0))\n        m_i = m_new\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        x = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, tl.exp(x - m_i) / l_i, mask=m)\ndef run(x):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, y, x.stride(0), N, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "593664d52d241680", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v3)"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + cols, mask=m, other=-float(\"inf\")).to(tl.float32) * sc\n    x = x - tl.max(x)\n    e = tl.where(m, tl.exp(x), 0.0)\n    tl.store(y_ptr + cols, e / tl.sum(e), mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "000bda712fefb803", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + cols, mask=m, other=-float(\"inf\")).to(tl.float32) * sc\n    x = x - tl.max(x)\n    e = tl.where(m, tl.exp(x), 0.0)\n    tl.store(y_ptr + cols, e / tl.sum(e), mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "bb338de608664f79", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + cols, mask=m, other=-float(\"inf\")).to(tl.float32) * sc\n    x = x - tl.max(x)\n    e = tl.where(m, tl.exp(x), 0.0)\n    tl.store(y_ptr + cols, e / tl.sum(e), mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "7dcd750cf85313d1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    cols = tl.arange(0, BLOCK); m = cols < N\n    x = tl.load(x_ptr + cols, mask=m, other=-float(\"inf\")).to(tl.float32) * sc\n    x = x - tl.max(x)\n    e = tl.where(m, tl.exp(x), 0.0)\n    tl.store(y_ptr + cols, e / tl.sum(e), mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "5e160d9652c45d9f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        mx = tl.maximum(mx, tl.max(v))\n    d = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        d += tl.sum(tl.where(cols < N, tl.exp(v - mx), 0.0))\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        v = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) * sc\n        tl.store(y_ptr + cols, tl.exp(v - mx) / d, mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=1024)\n    return y\n", "khash": "0799505f48337eed", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        mx = tl.maximum(mx, tl.max(v))\n    d = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        d += tl.sum(tl.where(cols < N, tl.exp(v - mx), 0.0))\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        v = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) * sc\n        tl.store(y_ptr + cols, tl.exp(v - mx) / d, mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=256)\n    return y\n", "khash": "381b51f54d575bcc", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        mx = tl.maximum(mx, tl.max(v))\n    d = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        d += tl.sum(tl.where(cols < N, tl.exp(v - mx), 0.0))\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        v = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) * sc\n        tl.store(y_ptr + cols, tl.exp(v - mx) / d, mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=512)\n    return y\n", "khash": "438d3b57696d8ed6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        mx = tl.maximum(mx, tl.max(v))\n    d = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        d += tl.sum(tl.where(cols < N, tl.exp(v - mx), 0.0))\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        v = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) * sc\n        tl.store(y_ptr + cols, tl.exp(v - mx) / d, mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=2048)\n    return y\n", "khash": "36109e18f1e1e54a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        mx = tl.maximum(mx, tl.max(v))\n    d = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        d += tl.sum(tl.where(cols < N, tl.exp(v - mx), 0.0))\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        v = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) * sc\n        tl.store(y_ptr + cols, tl.exp(v - mx) / d, mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=4096)\n    return y\n", "khash": "f5aa5ed744fe112d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        mx = tl.maximum(mx, tl.max(v))\n    d = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        d += tl.sum(tl.where(cols < N, tl.exp(v - mx), 0.0))\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        v = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) * sc\n        tl.store(y_ptr + cols, tl.exp(v - mx) / d, mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "87cd456c1ebf8957", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        mx = tl.maximum(mx, tl.max(v))\n    d = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        d += tl.sum(tl.where(cols < N, tl.exp(v - mx), 0.0))\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        v = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) * sc\n        tl.store(y_ptr + cols, tl.exp(v - mx) / d, mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "f21926d5b16c2f69", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        mx = tl.maximum(mx, tl.max(v))\n    d = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        d += tl.sum(tl.where(cols < N, tl.exp(v - mx), 0.0))\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        v = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) * sc\n        tl.store(y_ptr + cols, tl.exp(v - mx) / d, mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "e41d6d39347c3613", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        mx = tl.maximum(mx, tl.max(v))\n    d = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        d += tl.sum(tl.where(cols < N, tl.exp(v - mx), 0.0))\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        v = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) * sc\n        tl.store(y_ptr + cols, tl.exp(v - mx) / d, mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=256, num_warps=4)\n    return y\n", "khash": "386ce3233d0b220f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        mx = tl.maximum(mx, tl.max(v))\n    d = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        d += tl.sum(tl.where(cols < N, tl.exp(v - mx), 0.0))\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        v = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) * sc\n        tl.store(y_ptr + cols, tl.exp(v - mx) / d, mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=256, num_warps=8)\n    return y\n", "khash": "9e9b79712fe9e2ec", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        mx = tl.maximum(mx, tl.max(v))\n    d = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        d += tl.sum(tl.where(cols < N, tl.exp(v - mx), 0.0))\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        v = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) * sc\n        tl.store(y_ptr + cols, tl.exp(v - mx) / d, mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=256, num_stages=2)\n    return y\n", "khash": "28123c2d3a4e3aad", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        mx = tl.maximum(mx, tl.max(v))\n    d = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        d += tl.sum(tl.where(cols < N, tl.exp(v - mx), 0.0))\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        v = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) * sc\n        tl.store(y_ptr + cols, tl.exp(v - mx) / d, mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=512, num_warps=4)\n    return y\n", "khash": "5f08e4027859621d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        mx = tl.maximum(mx, tl.max(v))\n    d = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        d += tl.sum(tl.where(cols < N, tl.exp(v - mx), 0.0))\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        v = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) * sc\n        tl.store(y_ptr + cols, tl.exp(v - mx) / d, mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=512, num_warps=8)\n    return y\n", "khash": "dff96f0da605e812", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        mx = tl.maximum(mx, tl.max(v))\n    d = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        d += tl.sum(tl.where(cols < N, tl.exp(v - mx), 0.0))\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        v = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) * sc\n        tl.store(y_ptr + cols, tl.exp(v - mx) / d, mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=512, num_stages=2)\n    return y\n", "khash": "4975e3578c30b573", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        mx = tl.maximum(mx, tl.max(v))\n    d = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        d += tl.sum(tl.where(cols < N, tl.exp(v - mx), 0.0))\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        v = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) * sc\n        tl.store(y_ptr + cols, tl.exp(v - mx) / d, mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "35ffaf1bb10d0316", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        mx = tl.maximum(mx, tl.max(v))\n    d = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        d += tl.sum(tl.where(cols < N, tl.exp(v - mx), 0.0))\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        v = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) * sc\n        tl.store(y_ptr + cols, tl.exp(v - mx) / d, mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "854fe111fd52995b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        mx = tl.maximum(mx, tl.max(v))\n    d = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        d += tl.sum(tl.where(cols < N, tl.exp(v - mx), 0.0))\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        v = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) * sc\n        tl.store(y_ptr + cols, tl.exp(v - mx) / d, mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "c88b9b158ba4c82c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        mx = tl.maximum(mx, tl.max(v))\n    d = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        d += tl.sum(tl.where(cols < N, tl.exp(v - mx), 0.0))\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        v = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) * sc\n        tl.store(y_ptr + cols, tl.exp(v - mx) / d, mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "6bd4f12da2e939b9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        mx = tl.maximum(mx, tl.max(v))\n    d = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        d += tl.sum(tl.where(cols < N, tl.exp(v - mx), 0.0))\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        v = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) * sc\n        tl.store(y_ptr + cols, tl.exp(v - mx) / d, mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "d17d634bae9098e6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "softmax_scale", "src": "\n@triton.jit\ndef _k(x_ptr, s_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    sc = tl.load(s_ptr).to(tl.float32)\n    mx = -float(\"inf\")\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        mx = tl.maximum(mx, tl.max(v))\n    d = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK)\n        v = tl.load(x_ptr + cols, mask=cols < N, other=-float(\"inf\")).to(tl.float32) * sc\n        d += tl.sum(tl.where(cols < N, tl.exp(v - mx), 0.0))\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        v = tl.load(x_ptr + cols, mask=m, other=0.0).to(tl.float32) * sc\n        tl.store(y_ptr + cols, tl.exp(v - mx) / d, mask=m)\ndef run(x, scale):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, scale, y, x.stride(0), N, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "370cd99c455b390b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, (g * (1.0 / (1.0 + tl.exp(-g)))) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=1024)\n    return y\n", "khash": "cf6c124343e01712", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, (g * (1.0 / (1.0 + tl.exp(-g)))) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=2048)\n    return y\n", "khash": "8cda8ac5b4c9b3e9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, (g * (1.0 / (1.0 + tl.exp(-g)))) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=4096)\n    return y\n", "khash": "f815017b0f634f23", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, (g * (1.0 / (1.0 + tl.exp(-g)))) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "b8305c3a5cad134f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, (g * (1.0 / (1.0 + tl.exp(-g)))) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "69978ef2e6eeb048", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, (g * (1.0 / (1.0 + tl.exp(-g)))) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "6cc43cd660ff483c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, (g * (1.0 / (1.0 + tl.exp(-g)))) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "392aa12758324e40", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, (g * (1.0 / (1.0 + tl.exp(-g)))) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "9ed507cbeda8886f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, (g * (1.0 / (1.0 + tl.exp(-g)))) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "39c3e664b935ee62", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, (g * (1.0 / (1.0 + tl.exp(-g)))) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "a7bbacef99a7e964", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, (g * (1.0 / (1.0 + tl.exp(-g)))) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "0658b3a69f971513", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, (g * (1.0 / (1.0 + tl.exp(-g)))) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "2c7277b7ac431478", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=1024)\n    return y\n", "khash": "2574fc93dd0c8318", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=2048)\n    return y\n", "khash": "d6d8f6406c1a8c50", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=4096)\n    return y\n", "khash": "e88f71e70b54d19d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "7c0fa9002490dfc1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "90370be0d65daba0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "bc7259091c4f5a63", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "56ca5ef2335b5061", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "5f801d3752ed7184", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "85a5ffd440d5d841", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "b99795199cdbd596", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "363c08192cd0abea", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, n, BLOCK: tl.constexpr):\n    cols = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK); m = cols < n\n    g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n    tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    y = torch.empty_like(gate); n = gate.numel()\n    _k[(triton.cdiv(n, 1024),)](gate, up, y, n, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "be4cc5fc6363ffc2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4: tl.constexpr):\n    v5 = tl.program_id(v6)"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=1024)\n    return y\n", "khash": "8df3264f0f9af761", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=256)\n    return y\n", "khash": "6f48ad0bb842dc96", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=512)\n    return y\n", "khash": "8ec58c9835d3f1bf", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=2048)\n    return y\n", "khash": "2b506fe190f42640", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=4096)\n    return y\n", "khash": "2f10fd42b14e5292", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "47cb4ef9d4ada056", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "9b78e88250cfcd4e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "180167e877b578f0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=256, num_warps=4)\n    return y\n", "khash": "ba6874a9b45172af", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=256, num_warps=8)\n    return y\n", "khash": "f3503d83ff75cc2e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=256, num_stages=2)\n    return y\n", "khash": "ad2cfb8d838307cb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=512, num_warps=4)\n    return y\n", "khash": "842f458d7fa50c0b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=512, num_warps=8)\n    return y\n", "khash": "84626bdd97cd089f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=512, num_stages=2)\n    return y\n", "khash": "3b95947f4121b8de", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "b48ba5fb510edaca", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "9168de4dcb6e3b9c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "bd145b361a0c4e0e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "27d4917cf813c260", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "6f685a46f8d835bd", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "swiglu", "src": "\n@triton.jit\ndef _k(g_ptr, u_ptr, y_ptr, stride, N, BLOCK: tl.constexpr):\n    row = tl.program_id(0); g_ptr += row * stride; u_ptr += row * stride; y_ptr += row * stride\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); m = cols < N\n        g = tl.load(g_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        u = tl.load(u_ptr + cols, mask=m, other=0.0).to(tl.float32)\n        tl.store(y_ptr + cols, g * tl.sigmoid(g) * u, mask=m)\ndef run(gate, up):\n    M, N = gate.shape; y = torch.empty_like(gate)\n    _k[(M,)](gate, up, y, gate.stride(0), N, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "5c21aa5776a71a4f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5: tl.constexpr):\n    v6 = tl.program_id"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024)\n    return y\n", "khash": "b2ba50c6d5fc3008", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256)\n    return y\n", "khash": "54acedbe67a4f711", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512)\n    return y\n", "khash": "b7640e7fa5d2764c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048)\n    return y\n", "khash": "3d2967c78fd467ff", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096)\n    return y\n", "khash": "017d09ce757cd476", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "d1c447be4335d128", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "0c31a3391c4698a7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "98d8d976fc56edc0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=4)\n    return y\n", "khash": "e8be0543c6a393cb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=8)\n    return y\n", "khash": "a84777aa162d29a2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_stages=2)\n    return y\n", "khash": "86c9e79058867529", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=4)\n    return y\n", "khash": "a90c23bc497cb517", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=8)\n    return y\n", "khash": "4936a225d2242312", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_stages=2)\n    return y\n", "khash": "4ba5b0510c6ffcc1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "95c1d0f51ed3157f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "e40ee9beaa3d9a60", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "a4eea5ad212a785f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "987f3119a8b8b42b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "4297c8d0d9705bf0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "99dcc94d9ed0eece", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "92620095f3e31080", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "2788a5d83770eb7f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "9fa03844d8ce94dd", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "6fe16668953a5c86", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024)\n    return y\n", "khash": "26b3b41470c923d4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256)\n    return y\n", "khash": "11cabf45eb3562d2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512)\n    return y\n", "khash": "db1ad1ffc4fb34d4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048)\n    return y\n", "khash": "e934eca3a32cd018", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096)\n    return y\n", "khash": "9d30f2a41d96483a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "fcbec0efc458be4a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "41f2504c6c522122", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "8b8d22205eaff26d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=4)\n    return y\n", "khash": "6b75c55ac2998fae", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=8)\n    return y\n", "khash": "34f39a285aba45d0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_stages=2)\n    return y\n", "khash": "5bc1480e8fdeafab", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=4)\n    return y\n", "khash": "3cbf20f9d57024df", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=8)\n    return y\n", "khash": "236ddc2209f7fa07", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_stages=2)\n    return y\n", "khash": "99ee97a6779864ec", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "5db4cb52cbee19cf", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "da6b05374953d8f2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "ff739ecba4f2aa8e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "6351efd299b7b20c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "24f1e63f9c88ef42", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "9ec6d9fffbf0ccb1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "d9aba0b6e68070c6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "62e99fddbc1808a1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "7fbdf19ececffbbe", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "218982be90418ac5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024)\n    return y\n", "khash": "a72b9e15eeda6513", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256)\n    return y\n", "khash": "2468394f407991f3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512)\n    return y\n", "khash": "6465373af29198fd", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048)\n    return y\n", "khash": "6058c4c778e6d848", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096)\n    return y\n", "khash": "dd86acdc772b8604", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "e3dfd56fd2c9b307", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "e18233557f014147", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "67adb56882175fd7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=4)\n    return y\n", "khash": "0d13c56f4bd4a610", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=8)\n    return y\n", "khash": "ec1c293dc6427661", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_stages=2)\n    return y\n", "khash": "242b854773a0748f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=4)\n    return y\n", "khash": "0e26b6ad9c57a16e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=8)\n    return y\n", "khash": "182a0c346dc6152c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_stages=2)\n    return y\n", "khash": "31ecef8266535834", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "bcf124123139e318", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "da791de29b8f81b2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "93e544a45ce19e78", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "6d1801391ffe21ac", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "e05512d9dd3a968a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "ae9a0148ac749f5a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "9edc101c0c077a51", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "9abfa5f7eee2f241", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "986590496983818a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "df9c5d70045680d6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024)\n    return y\n", "khash": "4be9023d5468b869", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256)\n    return y\n", "khash": "8e1cada3e8f4bb44", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512)\n    return y\n", "khash": "ab745cc0bcd08d57", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048)\n    return y\n", "khash": "d4933e70bb387f79", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096)\n    return y\n", "khash": "ce3fc2634f6651f0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "29109a88a4c858d4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "609c3332dc866e05", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "155860e15f184cc2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=4)\n    return y\n", "khash": "184b26a25999c07f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=8)\n    return y\n", "khash": "09a4e1678e9f62a1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_stages=2)\n    return y\n", "khash": "9f92292834f5affb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=4)\n    return y\n", "khash": "8fda9d0ca65819ea", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=8)\n    return y\n", "khash": "4e4f5ad5069613e7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_stages=2)\n    return y\n", "khash": "bc22d2cfcf1edcfa", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "3f79ea52010b4a7b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "8bd0a81572ef9278", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "c83e6b9d71012fe2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "91e24278a74031b4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "b5733cd8811f1f7a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "e03ed5cb943e6077", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "959a33caeb8407e7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "d2d432ee4faa83b9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "168ff4ea7e1a07fc", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "7f6b214449d765e4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6: tl.constexpr):\n    v7 = tl.progra"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024)\n    return y\n", "khash": "a0ed92eb361d62c2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256)\n    return y\n", "khash": "c1b9734c1541930b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512)\n    return y\n", "khash": "741d19ca900f9334", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048)\n    return y\n", "khash": "f7b0dcf54f698f8a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096)\n    return y\n", "khash": "8e7866150b3df4a6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "f34a1c7338c09cb9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "61cd817e0a365ee7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "788ce5f3646aee8b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=4)\n    return y\n", "khash": "d7453c79849192ed", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=8)\n    return y\n", "khash": "0aa9b6a2c06d8703", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_stages=2)\n    return y\n", "khash": "8db6caaa6cc27a02", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=4)\n    return y\n", "khash": "f7c40f0708a8548f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=8)\n    return y\n", "khash": "01e7b5168e3f7767", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_stages=2)\n    return y\n", "khash": "5b20d01ee08c18e0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "eed20acbb581246d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "5ca902005a88d1df", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "3e0acc6d0f6561f5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "2b70ee3b90a9df5f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "2a319f83f38944b3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "c8d379ff1e4f3c38", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "354c4188f9fbb7c8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "fbdd44633a576501", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "030b336d6c5c6e2f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "b0854970586e218f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024)\n    return y\n", "khash": "56797f13ea3217c5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256)\n    return y\n", "khash": "967cbc513aaf6d99", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512)\n    return y\n", "khash": "eb9473101720ed0f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048)\n    return y\n", "khash": "1dcf636da3e9de10", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096)\n    return y\n", "khash": "eafa5e39aaa0dae2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "026a8ae2468a29cd", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "22f618d9414c692f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "d878865ada283c40", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=4)\n    return y\n", "khash": "2400536c5d387d43", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=8)\n    return y\n", "khash": "3a951390df8e8173", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_stages=2)\n    return y\n", "khash": "71ed48762e103521", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=4)\n    return y\n", "khash": "51065256ed917100", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=8)\n    return y\n", "khash": "b1eea6f1e6a7ab95", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_stages=2)\n    return y\n", "khash": "14020fed611e4c5a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "0b4185c28075fcc4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "9a35f02270cd303b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "d3a41ab32898be57", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "694e14b3c69128e7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "9269435e2c4dcc20", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "d2fc6d920993c060", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "f39fa47cb15d61ab", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "04aeb098b23405f5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "cd0aed1bc36daa59", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "64fec05215e786a7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024)\n    return y\n", "khash": "577fcc4a3d648e53", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256)\n    return y\n", "khash": "42836e72c8992a69", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512)\n    return y\n", "khash": "1895c5b6ce78d3b7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048)\n    return y\n", "khash": "eb29082c88f97a97", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096)\n    return y\n", "khash": "7e4deee4afd9b063", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "06344ba80c6eed17", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "5caa5a999eaebd75", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "b5b19b06895ac015", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=4)\n    return y\n", "khash": "3ebd67dd8b6769ce", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=8)\n    return y\n", "khash": "ef20e8cc5e5b9c93", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_stages=2)\n    return y\n", "khash": "9bbe3be56b4d015a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=4)\n    return y\n", "khash": "5f5f13c4327c2a07", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=8)\n    return y\n", "khash": "f459c2368c62f03b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_stages=2)\n    return y\n", "khash": "e90c5684e79b2f68", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "989eef996ea96368", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "b08949b4e63a01ee", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "78fbf04938d2a2fb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "406f2609d1dd18f0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "b62fb2d767a1fed5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "9d010efbc9509bd6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "b3ccc17206b6fc42", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "4bf6d2fb91ca3061", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "161e9aa60e5ad62d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "8d7170989891b36b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024)\n    return y\n", "khash": "3c896548b76fb771", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256)\n    return y\n", "khash": "670fddd3eabf0b95", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512)\n    return y\n", "khash": "8b28728f41e51dca", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048)\n    return y\n", "khash": "66a5a4bebb880338", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096)\n    return y\n", "khash": "c085d7b35c2d3df2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "751f9ef29027be29", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "e99c10357e393c81", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "fa8e6bebb474e05c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=4)\n    return y\n", "khash": "00640a004de886e5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_warps=8)\n    return y\n", "khash": "71f03b1fff230ce4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=256, num_stages=2)\n    return y\n", "khash": "54ee4463e5411d4e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=4)\n    return y\n", "khash": "82513f2c5d69ad7d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_warps=8)\n    return y\n", "khash": "be57e0366f31d380", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=512, num_stages=2)\n    return y\n", "khash": "3251cc72004c7d5d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "7310395530132d1c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "c1626f8d91dca4fd", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "85b9f96faf2e44bb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "da861da12d2064c9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "8f80ac80f4676570", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        s += tl.sum(h * h)\n    rr = tl.rsqrt(s / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = h * rr * w\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "d6d40ab3c5a9f530", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "17c26528d0f994a3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "93471949f294c263", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "1d1c67d17532bf73", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_rmsnorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    rr = tl.rsqrt(tl.sum(h * h) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = h * rr * w\n    tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, y, x.stride(0), N, 1e-06, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "6098b6274eb8efb9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024)\n    return y\n", "khash": "d3dcda3bc94d3a4b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256)\n    return y\n", "khash": "601bb7b8fafb3a19", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512)\n    return y\n", "khash": "d3730ea73fdb4aab", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048)\n    return y\n", "khash": "a07feb5c96100d08", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096)\n    return y\n", "khash": "60b67bf32d91d9b8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "9d7026b8eae17698", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "1073a2a5bb81a714", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "a50dcb89dd3d80a9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=4)\n    return y\n", "khash": "020f63e67d538097", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=8)\n    return y\n", "khash": "607388d1058d63e8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_stages=2)\n    return y\n", "khash": "a99e559a5e887955", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=4)\n    return y\n", "khash": "d073316ae18b45b7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=8)\n    return y\n", "khash": "a79156d0476bf07b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_stages=2)\n    return y\n", "khash": "1cacdd8203fbefb0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "fd39c7fdc7bd2a17", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "950498a5e9fa0300", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "7fd3bac8aa4ca64d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "4ab255c8e9c27271", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "aeb54baf64f5efee", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "baca18d3f8a98469", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "e41ae29d7a516297", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "0200375a86043e47", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "f38dd9f2cf90ff80", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "99aa0f89c9333e7f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024)\n    return y\n", "khash": "e4dece362896024f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256)\n    return y\n", "khash": "36a3df276491e0c2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512)\n    return y\n", "khash": "b501d26edc9f9b9b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048)\n    return y\n", "khash": "c9aa01434510aa6b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096)\n    return y\n", "khash": "503145f43426596e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "30506f1e8b1b47b0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "cf68414860823eb7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "e75b925b7cac8cf6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=4)\n    return y\n", "khash": "d660bee43de8c82e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=8)\n    return y\n", "khash": "5832cd50257015f4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_stages=2)\n    return y\n", "khash": "b891f8caf18995af", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=4)\n    return y\n", "khash": "cb01ba8d5858d59a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=8)\n    return y\n", "khash": "fe23c799017cf85d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_stages=2)\n    return y\n", "khash": "12c8aa3eec417885", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "b63c091d6fb8c1b7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "da2d8b81a0ed978f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "f012be05c12485ff", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "d03e03f4ed5de825", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "9d0af2453bbdee6a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "ca4e3af4ed4c1485", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "7c722f1b52b40869", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "ae8b2d501300774e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "b08dc701e97bc495", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "101ede0fb56daa96", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024)\n    return y\n", "khash": "0befd0de7001c0c0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256)\n    return y\n", "khash": "11bd14f64559efc0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512)\n    return y\n", "khash": "8393c5957438f81a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048)\n    return y\n", "khash": "b59f7d5ab91f8bd2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096)\n    return y\n", "khash": "6c5c2a8dd17000c8", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "baa60f16d5212f97", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "dae5980086a5d637", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "0e9b6db9c634b223", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=4)\n    return y\n", "khash": "2c1e922cee093a83", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=8)\n    return y\n", "khash": "8dcdb5dab84bd581", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_stages=2)\n    return y\n", "khash": "ac0a6d9bd6854dc5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=4)\n    return y\n", "khash": "6cce9473f7a3fde6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=8)\n    return y\n", "khash": "e42f7146d4b3ec57", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_stages=2)\n    return y\n", "khash": "87ae6eba07f48f85", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "88f6501761ae6b3a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "a8f491c05ebfb43d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "58f9321f0fa795a7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "9357bc4b2a02cc9a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "ecec3829116a08f0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "f018ce406f6111a2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "ccc323d4f57970a6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "ca3634603c1ea0d7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "edd6c31911782fac", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "2a6068605a97d60f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024)\n    return y\n", "khash": "4e0d8833b997cf2d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256)\n    return y\n", "khash": "0ce7c04bb942636d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512)\n    return y\n", "khash": "b1471fa3784fb811", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048)\n    return y\n", "khash": "c8180fe201f0e0ca", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096)\n    return y\n", "khash": "502a5d1a51c3b99c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "84e6858689a809f7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "d0e087f3ed80a93e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "f68099acaef71897", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=4)\n    return y\n", "khash": "4abf7dea24b106f6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=8)\n    return y\n", "khash": "816aea1501cdce23", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_stages=2)\n    return y\n", "khash": "d4407c61ed24b97c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=4)\n    return y\n", "khash": "0c9ad5a82fe74e44", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=8)\n    return y\n", "khash": "ed57e2f43b6b3b35", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_stages=2)\n    return y\n", "khash": "e30f421c9e2df741", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "6a3588cf60c8ffad", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "35205a53f739ae61", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "9822a6d2b9421bb7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "11d9041bb24eda20", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "e48b2de1bd468653", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "3de19e287e8abe9a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "eb672410825d97bb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "2fce85928058905a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "4bddbf1869d6d499", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "c260d51e70d48c4a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7: tl.constexpr):\n    v8 = tl.pr"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024)\n    return y\n", "khash": "e94a02907834bf32", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256)\n    return y\n", "khash": "e2fa79c0dcf2f337", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512)\n    return y\n", "khash": "f4f0b21db42f2006", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048)\n    return y\n", "khash": "812a14d2c39000c2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096)\n    return y\n", "khash": "b0ac73ef028a6813", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "cce8441ef54600a2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "09bf16729c4cf661", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "38755e5f44715ae0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=4)\n    return y\n", "khash": "1e1b67c6aaef722f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=8)\n    return y\n", "khash": "c551fe579affad6d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_stages=2)\n    return y\n", "khash": "a470bce51a311cf2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=4)\n    return y\n", "khash": "5ff26b01fe5cd8cc", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=8)\n    return y\n", "khash": "0b65631d55fa4de3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_stages=2)\n    return y\n", "khash": "806c5274c4d6a87e", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "0298c089e458310d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "9372de0f65119323", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "2a1fa4db5af28b0f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "6be2a5b842b32054", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "e03e378a4ed2388f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "24214cd304585d44", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "4bbf0ad0b17bac8d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "92b04009eb17d420", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "327470b040d445b0", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_tanh", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (2.0 * tl.sigmoid(2.0 * n) - 1.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "3fc51f5df6c41bd2", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024)\n    return y\n", "khash": "634c4d3535cf1cf4", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256)\n    return y\n", "khash": "b167ef2d3e3e3fbd", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512)\n    return y\n", "khash": "176b1d0d30ce7f49", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048)\n    return y\n", "khash": "d1aafebb92c86a23", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096)\n    return y\n", "khash": "9938fdac0a860adc", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "829192a08e7c3e64", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "1981f46a40d2ebab", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "7b136b4df40e6f00", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=4)\n    return y\n", "khash": "bfb2fb90bd5d2533", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=8)\n    return y\n", "khash": "14bdab3b8d62a3c1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_stages=2)\n    return y\n", "khash": "127a4039784457b3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=4)\n    return y\n", "khash": "347601fc42c6c318", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=8)\n    return y\n", "khash": "3a88408f17dacd8a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_stages=2)\n    return y\n", "khash": "575882fa133383f7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "69ddfb636ff3613a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "2e8220762e9e7c8c", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "51b40a19fba74922", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "eb9df7c78e8bf513", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "c69dcb7dd1a75d08", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "57be9099090c3230", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "10c67c7d09c02f39", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "e27c5e32d7163a13", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "5905a8ec76ff1fdc", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_sigmoid", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, tl.sigmoid(n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "760276d251a367a9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024)\n    return y\n", "khash": "0c179d5ff8fd3877", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256)\n    return y\n", "khash": "062d0f1699189c61", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512)\n    return y\n", "khash": "1a9f35ed651fa20b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048)\n    return y\n", "khash": "d075b4d29ca81470", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096)\n    return y\n", "khash": "4c01c9e185622b5a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "50081abea872174b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "1d8484d3390de0dc", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "8c400ca1a576e6dc", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=4)\n    return y\n", "khash": "71439a0b59988de5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=8)\n    return y\n", "khash": "cfebc7565436ce67", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_stages=2)\n    return y\n", "khash": "20a0f44d83a36fef", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=4)\n    return y\n", "khash": "2fc88a9d1a8bdf53", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=8)\n    return y\n", "khash": "d36404a7c15bb6db", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_stages=2)\n    return y\n", "khash": "fdabce1f31c2fd15", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "1087939086bfb949", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "1b0599b892fdf253", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "08d7ac584411089f", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "b56598776f867f6d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "ab6c4fd040f0c476", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "5951d43c21a51d5a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "d5890e7669095fc3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "be2e5ac0585624d9", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "be972a89c1073700", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_relu", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, tl.maximum(n, 0.0), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "a4a24d137ec7734d", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024)\n    return y\n", "khash": "23702780bfdeffd1", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256)\n    return y\n", "khash": "bfceae0d6b4bbcb3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512)\n    return y\n", "khash": "0c0c9be93e6a3eec", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048)\n    return y\n", "khash": "cc8f10be903c83eb", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096)\n    return y\n", "khash": "6f1317b469dbd840", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=4)\n    return y\n", "khash": "6cb84e8d83909a59", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_warps=8)\n    return y\n", "khash": "c2caf431b969c4c7", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=1024, num_stages=2)\n    return y\n", "khash": "d3d5db41399dc392", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=4)\n    return y\n", "khash": "d315b328e4a53499", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_warps=8)\n    return y\n", "khash": "f341d7590acb576b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=256, num_stages=2)\n    return y\n", "khash": "84bdf681735c439a", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=4)\n    return y\n", "khash": "e59ae25e0f182b59", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_warps=8)\n    return y\n", "khash": "2fc80e4859733307", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=512, num_stages=2)\n    return y\n", "khash": "9b7bf6dccd3ef317", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=4)\n    return y\n", "khash": "a338b7d43d417c78", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_warps=8)\n    return y\n", "khash": "bba53d324d5f62ee", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=2048, num_stages=2)\n    return y\n", "khash": "b1af1ec40beba3a5", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=4)\n    return y\n", "khash": "df586a477d9b9c25", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_warps=8)\n    return y\n", "khash": "885d7923eb7746ae", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    s = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        s += tl.sum(tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32))\n    mu = s / N\n    v = 0.0\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        d = tl.where(MM, (tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)) - mu, 0.0); v += tl.sum(d * d)\n    rr = tl.rsqrt(v / N + eps)\n    for off in range(0, N, BLOCK):\n        cols = off + tl.arange(0, BLOCK); MM = cols < N\n        h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n        n = (h - mu) * rr * w + b\n        tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=4096, num_stages=2)\n    return y\n", "khash": "805939f9e5ca4d94", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N))\n    return y\n", "khash": "97ecb1d489531fc3", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=4)\n    return y\n", "khash": "848f07e87f390b2b", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_warps=8)\n    return y\n", "khash": "cb12ab027689a0d6", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}
{"op": "add_layernorm_square", "src": "@triton.jit\ndef _k(x_ptr, r_ptr, w_ptr, b_ptr, y_ptr, stride, N, eps, BLOCK: tl.constexpr):\n    row = tl.program_id(0); x_ptr += row * stride; r_ptr += row * stride; y_ptr += row * stride\n    cols = tl.arange(0, BLOCK); MM = cols < N\n    h = tl.load(x_ptr + cols, mask=MM, other=0.0).to(tl.float32) + tl.load(r_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    mu = tl.sum(h) / N\n    hc = tl.where(MM, h - mu, 0.0)\n    rr = tl.rsqrt(tl.sum(hc * hc) / N + eps)\n    w = tl.load(w_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + cols, mask=MM, other=0.0).to(tl.float32)\n    n = hc * rr * w + b\n    tl.store(y_ptr + cols, (n * n), mask=MM)\ndef run(x, residual, w, b):\n    M, N = x.shape; y = torch.empty_like(x)\n    _k[(M,)](x, residual, w, b, y, x.stride(0), N, 1e-05, BLOCK=triton.next_power_of_2(N), num_stages=2)\n    return y\n", "khash": "ed469fd2d3e630ab", "struct": "@triton.jit\ndef _k(v0, v1, v2, v3, v4, v5, v6, v7, v8: tl.constexpr):\n    v9 = t"}